// url/mojom/scheme_host_port.mojom-blink.h is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2013 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

#ifndef URL_MOJOM_SCHEME_HOST_PORT_MOJOM_BLINK_H_
#define URL_MOJOM_SCHEME_HOST_PORT_MOJOM_BLINK_H_

#include <stdint.h>

#include <limits>
#include <optional>
#include <type_traits>
#include <utility>

#include "base/types/cxx23_to_underlying.h"
#include "mojo/public/cpp/bindings/clone_traits.h"
#include "mojo/public/cpp/bindings/equals_traits.h"
#include "mojo/public/cpp/bindings/lib/serialization.h"
#include "mojo/public/cpp/bindings/struct_ptr.h"
#include "mojo/public/cpp/bindings/struct_traits.h"
#include "mojo/public/cpp/bindings/union_traits.h"

#include "third_party/perfetto/include/perfetto/tracing/traced_value_forward.h"

#include "url/mojom/scheme_host_port.mojom-features.h" // IWYU pragma: export
#include "url/mojom/scheme_host_port.mojom-shared.h" // IWYU pragma: export
#include "url/mojom/scheme_host_port.mojom-blink-forward.h" // IWYU pragma: export

#include "mojo/public/cpp/bindings/lib/wtf_clone_equals_util.h"
#include "mojo/public/cpp/bindings/lib/wtf_hash_util.h"
#include "third_party/blink/renderer/platform/wtf/hash_functions.h"
#include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"

#include "url/mojom/scheme_host_port_mojom_traits.h"
#if !BLINK_MOJO_IMPL && !INSIDE_BLINK
#error "File must only be imported inside blink"
#endif

namespace url::mojom::blink {

class SchemeHostPort {
public:
    template <typename T> using EnableIfSame = std::enable_if_t<std::is_same<SchemeHostPort, T>::value>;
    using DataView = SchemeHostPortDataView;
    using Data_ = internal::SchemeHostPort_Data;

    template <typename... Args> static SchemeHostPortPtr New(Args&&... args)
    {
        return SchemeHostPortPtr(std::in_place, std::forward<Args>(args)...);
    }

    template <typename U> static SchemeHostPortPtr From(const U& u)
    {
        return mojo::TypeConverter<SchemeHostPortPtr, U>::Convert(u);
    }

    template <typename U> U To() const
    {
        return mojo::TypeConverter<U, SchemeHostPort>::Convert(*this);
    }

    SchemeHostPort()
    : scheme()
    , host()
    , port()
{
}

    SchemeHostPort(const WTF::String& scheme_in, const WTF::String& host_in, uint16_t port_in)
    : scheme(std::move(scheme_in))
    , host(std::move(host_in))
    , port(std::move(port_in)){
}

    ~SchemeHostPort() = default;

    // Clone() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Clone() or copy
    // constructor/assignment are available for members.
    template <typename StructPtrType = SchemeHostPortPtr> SchemeHostPortPtr Clone() const;

    // Equals() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Equals() or == operator
    // are available for members.
    template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool Equals(const T& other) const;

    template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool operator==(const T& rhs) const
    {
        return Equals(rhs);
    }

    template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool operator!=(const T& rhs) const
    {
        return !operator==(rhs);
    }
    template <typename UserType> static WTF::Vector<uint8_t> Serialize(UserType* input)
    {
        return mojo::internal::SerializeImpl<SchemeHostPort::DataView, WTF::Vector<uint8_t>>(input);
    }

    template <typename UserType> static mojo::Message SerializeAsMessage(UserType* input)
    {
        return mojo::internal::SerializeAsMessageImpl<SchemeHostPort::DataView>(input);
    }

    // The returned Message is serialized only if the message is moved
    // cross-process or cross-language. Otherwise if the message is Deserialized
    // as the same UserType |input| will just be moved to |output| in
    // DeserializeFromMessage.
    template <typename UserType> static mojo::Message WrapAsMessage(UserType input)
    {
        return mojo::Message(std::make_unique<internal::SchemeHostPort_UnserializedMessageContext<UserType, SchemeHostPort::DataView>>(0, 0, std::move(input)),
            MOJO_CREATE_MESSAGE_FLAG_NONE);
    }

    template <typename UserType> static bool Deserialize(const void* data, size_t data_num_bytes, UserType* output)
    {
        mojo::Message message;
        return mojo::internal::DeserializeImpl<SchemeHostPort::DataView>(message, data, data_num_bytes, output, Validate);
    }

    template <typename UserType> static bool Deserialize(base::span<const uint8_t> input, UserType* output)
    {
        return SchemeHostPort::Deserialize(input.empty() ? nullptr : input.data(), input.size(), output);
    }

    template <typename UserType> static bool DeserializeFromMessage(mojo::Message input, UserType* output)
    {
        auto context = input.TakeUnserializedContext<internal::SchemeHostPort_UnserializedMessageContext<UserType, SchemeHostPort::DataView>>();
        if (context) {
            *output = std::move(context->TakeData());
            return true;
        }
        input.SerializeIfNecessary();
        return mojo::internal::DeserializeImpl<SchemeHostPort::DataView>(input, input.payload(), input.payload_num_bytes(), output, Validate);
    }

    WTF::String scheme;

    WTF::String host;

    uint16_t port;

    // Serialise this struct into a trace.
    void WriteIntoTrace(perfetto::TracedValue traced_context) const;

private:
    static bool Validate(const void* data, mojo::internal::ValidationContext* validation_context);
};

// The comparison operators are templates, so they are only instantiated if they
// are used. Thus, the bindings generator does not need to know whether
// comparison operators are available for members.
template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool operator<(const T& lhs, const T& rhs);

template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool operator<=(const T& lhs, const T& rhs)
{
    return !(rhs < lhs);
}

template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool operator>(const T& lhs, const T& rhs)
{
    return rhs < lhs;
}

template <typename T, SchemeHostPort::EnableIfSame<T>* = nullptr> bool operator>=(const T& lhs, const T& rhs)
{
    return !(lhs < rhs);
}

template <typename StructPtrType> SchemeHostPortPtr SchemeHostPort::Clone() const
{
    return New(mojo::Clone(scheme), mojo::Clone(host), mojo::Clone(port));
}

template <typename T, SchemeHostPort::EnableIfSame<T>*> bool SchemeHostPort::Equals(const T& other_struct) const
{
    if (!mojo::Equals(this->scheme, other_struct.scheme))
        return false;
    if (!mojo::Equals(this->host, other_struct.host))
        return false;
    if (!mojo::Equals(this->port, other_struct.port))
        return false;
    return true;
}

template <typename T, SchemeHostPort::EnableIfSame<T>*> bool operator<(const T& lhs, const T& rhs)
{
    if (lhs.scheme < rhs.scheme)
        return true;
    if (rhs.scheme < lhs.scheme)
        return false;
    if (lhs.host < rhs.host)
        return true;
    if (rhs.host < lhs.host)
        return false;
    if (lhs.port < rhs.port)
        return true;
    if (rhs.port < lhs.port)
        return false;
    return false;
}

} // url::mojom::blink

namespace mojo {

template <> struct StructTraits<::url::mojom::blink::SchemeHostPort::DataView, ::url::mojom::blink::SchemeHostPortPtr> {
    static bool IsNull(const ::url::mojom::blink::SchemeHostPortPtr& input)
    {
        return !input;
    }
    static void SetToNull(::url::mojom::blink::SchemeHostPortPtr* output)
    {
        output->reset();
    }

    static const decltype(::url::mojom::blink::SchemeHostPort::scheme)& scheme(const ::url::mojom::blink::SchemeHostPortPtr& input)
    {
        return input->scheme;
    }

    static const decltype(::url::mojom::blink::SchemeHostPort::host)& host(const ::url::mojom::blink::SchemeHostPortPtr& input)
    {
        return input->host;
    }

    static decltype(::url::mojom::blink::SchemeHostPort::port) port(const ::url::mojom::blink::SchemeHostPortPtr& input)
    {
        return input->port;
    }

    static bool Read(::url::mojom::blink::SchemeHostPort::DataView input, ::url::mojom::blink::SchemeHostPortPtr* output);
};

} // namespace mojo

#endif // URL_MOJOM_SCHEME_HOST_PORT_MOJOM_BLINK_H_
