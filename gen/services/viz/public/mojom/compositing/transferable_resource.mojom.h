// services/viz/public/mojom/compositing/transferable_resource.mojom.h is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2013 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

#ifndef SERVICES_VIZ_PUBLIC_MOJOM_COMPOSITING_TRANSFERABLE_RESOURCE_MOJOM_H_
#define SERVICES_VIZ_PUBLIC_MOJOM_COMPOSITING_TRANSFERABLE_RESOURCE_MOJOM_H_

#include <stdint.h>

#include <limits>
#include <optional>
#include <type_traits>
#include <utility>

#include "base/types/cxx23_to_underlying.h"
#include "mojo/public/cpp/bindings/clone_traits.h"
#include "mojo/public/cpp/bindings/equals_traits.h"
#include "mojo/public/cpp/bindings/lib/serialization.h"
#include "mojo/public/cpp/bindings/struct_ptr.h"
#include "mojo/public/cpp/bindings/struct_traits.h"
#include "mojo/public/cpp/bindings/union_traits.h"

#include "third_party/perfetto/include/perfetto/tracing/traced_value_forward.h"

#include "services/viz/public/mojom/compositing/transferable_resource.mojom-features.h" // IWYU pragma: export
#include "services/viz/public/mojom/compositing/transferable_resource.mojom-shared.h" // IWYU pragma: export
#include "services/viz/public/mojom/compositing/transferable_resource.mojom-forward.h" // IWYU pragma: export
#include "gpu/ipc/common/mailbox.mojom.h"
#include "gpu/ipc/common/sync_token.mojom.h"
#include "gpu/ipc/common/vulkan_ycbcr_info.mojom.h"
#include "services/viz/public/mojom/compositing/shared_image_format.mojom.h"
#include "services/viz/public/mojom/compositing/resource_id.mojom.h"
#include "services/viz/public/mojom/compositing/shared_bitmap_id.mojom.h"
#include "ui/gfx/geometry/mojom/geometry.mojom.h"
#include "ui/gfx/mojom/color_space.mojom.h"
#include "ui/gfx/mojom/hdr_metadata.mojom.h"
#include <string>
#include <vector>

#include "services/viz/public/cpp/compositing/transferable_resource_mojom_traits.h"

namespace viz::mojom {

class MemoryBufferId {
public:
    using DataView = MemoryBufferIdDataView;
    using Data_ = internal::MemoryBufferId_Data;
    using Tag = Data_::MemoryBufferId_Tag;

    template <typename... Args> static MemoryBufferIdPtr New(Args&&... args)
    {
        static_assert(sizeof...(args) < 0,
            "Do not use Union::New(); to create a union of a given subtype, use "
            "New<SubType>(), not New() followed by set_<sub_type>(). To represent "
            "an empty union, mark the field or parameter as nullable in the mojom "
            "definition.");
        return nullptr;
    }
    // Construct an instance holding |mailbox|.
    static MemoryBufferIdPtr NewMailbox(const ::gpu::Mailbox& value)
    {
        auto result = MemoryBufferIdPtr(std::in_place);
        result->set_mailbox(std::move(value));
        return result;
    }
    // Construct an instance holding |shared_bitmap_id|.
    static MemoryBufferIdPtr NewSharedBitmapId(const ::viz::SharedBitmapId& value)
    {
        auto result = MemoryBufferIdPtr(std::in_place);
        result->set_shared_bitmap_id(std::move(value));
        return result;
    }

    template <typename U> static MemoryBufferIdPtr From(const U& u)
    {
        return mojo::TypeConverter<MemoryBufferIdPtr, U>::Convert(u);
    }

    template <typename U> U To() const
    {
        return mojo::TypeConverter<U, MemoryBufferId>::Convert(*this);
    }

    MemoryBufferId()
    : tag_(Tag::kMailbox)
{
    data_.mailbox = new ::gpu::Mailbox;
}
    ~MemoryBufferId(){
    DestroyActive();
}
    // Delete the copy constructor and copy assignment operators because `data_`
    // contains raw pointers that must not be copied.
    MemoryBufferId(const MemoryBufferId& other) = delete;
    MemoryBufferId& operator=(const MemoryBufferId& other) = delete;

    // Clone() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Clone() or copy
    // constructor/assignment are available for members.
    template <typename UnionPtrType = MemoryBufferIdPtr> MemoryBufferIdPtr Clone() const;

    // Equals() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Equals() or == operator
    // are available for members.
    template <typename T, typename std::enable_if<std::is_same<T, MemoryBufferId>::value>::type* = nullptr> bool Equals(const T& other) const;

    template <typename T, typename std::enable_if<std::is_same<T, MemoryBufferId>::value>::type* = nullptr> bool operator==(const T& rhs) const
    {
        return Equals(rhs);
    }

    Tag which() const
    {
        return tag_;
    }

    bool is_mailbox() const
    {
        return tag_ == Tag::kMailbox;
    }

    ::gpu::Mailbox& get_mailbox() const
    {
        CHECK(tag_ == Tag::kMailbox);
        return *(data_.mailbox);
    }

    void set_mailbox(const ::gpu::Mailbox& mailbox);

    bool is_shared_bitmap_id() const
    {
        return tag_ == Tag::kSharedBitmapId;
    }

    ::viz::SharedBitmapId& get_shared_bitmap_id() const
    {
        CHECK(tag_ == Tag::kSharedBitmapId);
        return *(data_.shared_bitmap_id);
    }

    void set_shared_bitmap_id(const ::viz::SharedBitmapId& shared_bitmap_id);

    template <typename UserType> static mojo::Message SerializeAsMessage(UserType* input)
    {
        return mojo::internal::SerializeAsMessageImpl<MemoryBufferId::DataView>(input);
    }

    template <typename UserType> static bool DeserializeFromMessage(mojo::Message input, UserType* output)
    {
        return mojo::internal::DeserializeImpl<MemoryBufferId::DataView>(input, input.payload(), input.payload_num_bytes(), output, Validate);
    }

private:
    union Union_ {
        Union_() = default;
        ~Union_() = default;
        ::gpu::Mailbox* mailbox;
        ::viz::SharedBitmapId* shared_bitmap_id;
    };

    static bool Validate(const void* data, mojo::internal::ValidationContext* validation_context);

    void DestroyActive();
    Tag tag_;
    Union_ data_;
};

class TransferableResource {
public:
    template <typename T> using EnableIfSame = std::enable_if_t<std::is_same<TransferableResource, T>::value>;
    using DataView = TransferableResourceDataView;
    using Data_ = internal::TransferableResource_Data;

    template <typename... Args> static TransferableResourcePtr New(Args&&... args)
    {
        return TransferableResourcePtr(std::in_place, std::forward<Args>(args)...);
    }

    template <typename U> static TransferableResourcePtr From(const U& u)
    {
        return mojo::TypeConverter<TransferableResourcePtr, U>::Convert(u);
    }

    template <typename U> U To() const
    {
        return mojo::TypeConverter<U, TransferableResource>::Convert(*this);
    }

    TransferableResource()
    : id()
    , format()
    , size()
    , memory_buffer_id()
    , sync_token()
    , texture_target()
    , synchronization_type()
    , is_software()
    , is_overlay_candidate()
    , wants_promotion_hint()
    , color_space()
    , hdr_metadata()
    , needs_detiling()
    , ycbcr_info()
{
}

    TransferableResource(const ::viz::ResourceId& id_in, const ::viz::SharedImageFormat& format_in, const ::gfx::Size& size_in,
    MemoryBufferIdPtr memory_buffer_id_in, const ::gpu::SyncToken& sync_token_in, uint32_t texture_target_in, SynchronizationType synchronization_type_in,
    bool is_software_in, bool is_overlay_candidate_in, bool wants_promotion_hint_in, const ::gfx::ColorSpace& color_space_in,
    const ::gfx::HDRMetadata& hdr_metadata_in, bool needs_detiling_in, std::optional<::gpu::VulkanYCbCrInfo> ycbcr_info_in)
    : id(std::move(id_in))
    , format(std::move(format_in))
    , size(std::move(size_in))
    , memory_buffer_id(std::move(memory_buffer_id_in))
    , sync_token(std::move(sync_token_in))
    , texture_target(std::move(texture_target_in))
    , synchronization_type(std::move(synchronization_type_in))
    , is_software(std::move(is_software_in))
    , is_overlay_candidate(std::move(is_overlay_candidate_in))
    , wants_promotion_hint(std::move(wants_promotion_hint_in))
    , color_space(std::move(color_space_in))
    , hdr_metadata(std::move(hdr_metadata_in))
    , needs_detiling(std::move(needs_detiling_in))
    , ycbcr_info(std::move(ycbcr_info_in)){
}

    TransferableResource(const TransferableResource&) = delete;
    TransferableResource& operator=(const TransferableResource&) = delete;

    ~TransferableResource() = default;

    // Clone() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Clone() or copy
    // constructor/assignment are available for members.
    template <typename StructPtrType = TransferableResourcePtr> TransferableResourcePtr Clone() const;

    // Equals() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Equals() or == operator
    // are available for members.
    template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool Equals(const T& other) const;

    template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool operator==(const T& rhs) const
    {
        return Equals(rhs);
    }

    template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool operator!=(const T& rhs) const
    {
        return !operator==(rhs);
    }
    template <typename UserType> static std::vector<uint8_t> Serialize(UserType* input)
    {
        return mojo::internal::SerializeImpl<TransferableResource::DataView, std::vector<uint8_t>>(input);
    }

    template <typename UserType> static mojo::Message SerializeAsMessage(UserType* input)
    {
        return mojo::internal::SerializeAsMessageImpl<TransferableResource::DataView>(input);
    }

    // The returned Message is serialized only if the message is moved
    // cross-process or cross-language. Otherwise if the message is Deserialized
    // as the same UserType |input| will just be moved to |output| in
    // DeserializeFromMessage.
    template <typename UserType> static mojo::Message WrapAsMessage(UserType input)
    {
        return mojo::Message(
            std::make_unique<internal::TransferableResource_UnserializedMessageContext<UserType, TransferableResource::DataView>>(0, 0, std::move(input)),
            MOJO_CREATE_MESSAGE_FLAG_NONE);
    }

    template <typename UserType> static bool Deserialize(const void* data, size_t data_num_bytes, UserType* output)
    {
        mojo::Message message;
        return mojo::internal::DeserializeImpl<TransferableResource::DataView>(message, data, data_num_bytes, output, Validate);
    }

    template <typename UserType> static bool Deserialize(base::span<const uint8_t> input, UserType* output)
    {
        return TransferableResource::Deserialize(input.empty() ? nullptr : input.data(), input.size(), output);
    }

    template <typename UserType> static bool DeserializeFromMessage(mojo::Message input, UserType* output)
    {
        auto context = input.TakeUnserializedContext<internal::TransferableResource_UnserializedMessageContext<UserType, TransferableResource::DataView>>();
        if (context) {
            *output = std::move(context->TakeData());
            return true;
        }
        input.SerializeIfNecessary();
        return mojo::internal::DeserializeImpl<TransferableResource::DataView>(input, input.payload(), input.payload_num_bytes(), output, Validate);
    }

    ::viz::ResourceId id;

    ::viz::SharedImageFormat format;

    ::gfx::Size size;

    MemoryBufferIdPtr memory_buffer_id;

    ::gpu::SyncToken sync_token;

    uint32_t texture_target;

    SynchronizationType synchronization_type;

    bool is_software;

    bool is_overlay_candidate;

    bool wants_promotion_hint;

    ::gfx::ColorSpace color_space;

    ::gfx::HDRMetadata hdr_metadata;

    bool needs_detiling;

    std::optional<::gpu::VulkanYCbCrInfo> ycbcr_info;

    // Serialise this struct into a trace.
    void WriteIntoTrace(perfetto::TracedValue traced_context) const;

private:
    static bool Validate(const void* data, mojo::internal::ValidationContext* validation_context);
};

// The comparison operators are templates, so they are only instantiated if they
// are used. Thus, the bindings generator does not need to know whether
// comparison operators are available for members.
template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool operator<(const T& lhs, const T& rhs);

template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool operator<=(const T& lhs, const T& rhs)
{
    return !(rhs < lhs);
}

template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool operator>(const T& lhs, const T& rhs)
{
    return rhs < lhs;
}

template <typename T, TransferableResource::EnableIfSame<T>* = nullptr> bool operator>=(const T& lhs, const T& rhs)
{
    return !(lhs < rhs);
}

template <typename UnionPtrType> MemoryBufferIdPtr MemoryBufferId::Clone() const
{
    switch (tag_) {
    case Tag::kMailbox:
        return NewMailbox(mojo::Clone(*data_.mailbox));
    case Tag::kSharedBitmapId:
        return NewSharedBitmapId(mojo::Clone(*data_.shared_bitmap_id));
    }
    return nullptr;
}

template <typename T, typename std::enable_if<std::is_same<T, MemoryBufferId>::value>::type*> bool MemoryBufferId::Equals(const T& other) const
{
    if (tag_ != other.which())
        return false;

    switch (tag_) {
    case Tag::kMailbox:
        return mojo::Equals(*(data_.mailbox), *(other.data_.mailbox));
    case Tag::kSharedBitmapId:
        return mojo::Equals(*(data_.shared_bitmap_id), *(other.data_.shared_bitmap_id));
    }

    return false;
}
template <typename StructPtrType> TransferableResourcePtr TransferableResource::Clone() const
{
    return New(mojo::Clone(id), mojo::Clone(format), mojo::Clone(size), mojo::Clone(memory_buffer_id), mojo::Clone(sync_token), mojo::Clone(texture_target),
        mojo::Clone(synchronization_type), mojo::Clone(is_software), mojo::Clone(is_overlay_candidate), mojo::Clone(wants_promotion_hint),
        mojo::Clone(color_space), mojo::Clone(hdr_metadata), mojo::Clone(needs_detiling), mojo::Clone(ycbcr_info));
}

template <typename T, TransferableResource::EnableIfSame<T>*> bool TransferableResource::Equals(const T& other_struct) const
{
    if (!mojo::Equals(this->id, other_struct.id))
        return false;
    if (!mojo::Equals(this->format, other_struct.format))
        return false;
    if (!mojo::Equals(this->size, other_struct.size))
        return false;
    if (!mojo::Equals(this->memory_buffer_id, other_struct.memory_buffer_id))
        return false;
    if (!mojo::Equals(this->sync_token, other_struct.sync_token))
        return false;
    if (!mojo::Equals(this->texture_target, other_struct.texture_target))
        return false;
    if (!mojo::Equals(this->synchronization_type, other_struct.synchronization_type))
        return false;
    if (!mojo::Equals(this->is_software, other_struct.is_software))
        return false;
    if (!mojo::Equals(this->is_overlay_candidate, other_struct.is_overlay_candidate))
        return false;
    if (!mojo::Equals(this->wants_promotion_hint, other_struct.wants_promotion_hint))
        return false;
    if (!mojo::Equals(this->color_space, other_struct.color_space))
        return false;
    if (!mojo::Equals(this->hdr_metadata, other_struct.hdr_metadata))
        return false;
    if (!mojo::Equals(this->needs_detiling, other_struct.needs_detiling))
        return false;
    if (!mojo::Equals(this->ycbcr_info, other_struct.ycbcr_info))
        return false;
    return true;
}

template <typename T, TransferableResource::EnableIfSame<T>*> bool operator<(const T& lhs, const T& rhs)
{
    if (lhs.id < rhs.id)
        return true;
    if (rhs.id < lhs.id)
        return false;
    if (lhs.format < rhs.format)
        return true;
    if (rhs.format < lhs.format)
        return false;
    if (lhs.size < rhs.size)
        return true;
    if (rhs.size < lhs.size)
        return false;
    if (lhs.memory_buffer_id < rhs.memory_buffer_id)
        return true;
    if (rhs.memory_buffer_id < lhs.memory_buffer_id)
        return false;
    if (lhs.sync_token < rhs.sync_token)
        return true;
    if (rhs.sync_token < lhs.sync_token)
        return false;
    if (lhs.texture_target < rhs.texture_target)
        return true;
    if (rhs.texture_target < lhs.texture_target)
        return false;
    if (lhs.synchronization_type < rhs.synchronization_type)
        return true;
    if (rhs.synchronization_type < lhs.synchronization_type)
        return false;
    if (lhs.is_software < rhs.is_software)
        return true;
    if (rhs.is_software < lhs.is_software)
        return false;
    if (lhs.is_overlay_candidate < rhs.is_overlay_candidate)
        return true;
    if (rhs.is_overlay_candidate < lhs.is_overlay_candidate)
        return false;
    if (lhs.wants_promotion_hint < rhs.wants_promotion_hint)
        return true;
    if (rhs.wants_promotion_hint < lhs.wants_promotion_hint)
        return false;
    if (lhs.color_space < rhs.color_space)
        return true;
    if (rhs.color_space < lhs.color_space)
        return false;
    if (lhs.hdr_metadata < rhs.hdr_metadata)
        return true;
    if (rhs.hdr_metadata < lhs.hdr_metadata)
        return false;
    if (lhs.needs_detiling < rhs.needs_detiling)
        return true;
    if (rhs.needs_detiling < lhs.needs_detiling)
        return false;
    if (lhs.ycbcr_info < rhs.ycbcr_info)
        return true;
    if (rhs.ycbcr_info < lhs.ycbcr_info)
        return false;
    return false;
}

} // viz::mojom

namespace mojo {

template <> struct StructTraits<::viz::mojom::TransferableResource::DataView, ::viz::mojom::TransferableResourcePtr> {
    static bool IsNull(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return !input;
    }
    static void SetToNull(::viz::mojom::TransferableResourcePtr* output)
    {
        output->reset();
    }

    static const decltype(::viz::mojom::TransferableResource::id)& id(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->id;
    }

    static const decltype(::viz::mojom::TransferableResource::format)& format(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->format;
    }

    static const decltype(::viz::mojom::TransferableResource::size)& size(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->size;
    }

    static const decltype(::viz::mojom::TransferableResource::memory_buffer_id)& memory_buffer_id(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->memory_buffer_id;
    }

    static const decltype(::viz::mojom::TransferableResource::sync_token)& sync_token(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->sync_token;
    }

    static decltype(::viz::mojom::TransferableResource::texture_target) texture_target(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->texture_target;
    }

    static decltype(::viz::mojom::TransferableResource::synchronization_type) synchronization_type(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->synchronization_type;
    }

    static decltype(::viz::mojom::TransferableResource::is_software) is_software(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->is_software;
    }

    static decltype(::viz::mojom::TransferableResource::is_overlay_candidate) is_overlay_candidate(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->is_overlay_candidate;
    }

    static decltype(::viz::mojom::TransferableResource::wants_promotion_hint) wants_promotion_hint(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->wants_promotion_hint;
    }

    static const decltype(::viz::mojom::TransferableResource::color_space)& color_space(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->color_space;
    }

    static const decltype(::viz::mojom::TransferableResource::hdr_metadata)& hdr_metadata(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->hdr_metadata;
    }

    static decltype(::viz::mojom::TransferableResource::needs_detiling) needs_detiling(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->needs_detiling;
    }

    static const decltype(::viz::mojom::TransferableResource::ycbcr_info)& ycbcr_info(const ::viz::mojom::TransferableResourcePtr& input)
    {
        return input->ycbcr_info;
    }

    static bool Read(::viz::mojom::TransferableResource::DataView input, ::viz::mojom::TransferableResourcePtr* output);
};

template <> struct UnionTraits<::viz::mojom::MemoryBufferId::DataView, ::viz::mojom::MemoryBufferIdPtr> {
    static bool IsNull(const ::viz::mojom::MemoryBufferIdPtr& input)
    {
        return !input;
    }
    static void SetToNull(::viz::mojom::MemoryBufferIdPtr* output)
    {
        output->reset();
    }

    static ::viz::mojom::MemoryBufferId::Tag GetTag(const ::viz::mojom::MemoryBufferIdPtr& input)
    {
        return input->which();
    }

    static const ::gpu::Mailbox& mailbox(const ::viz::mojom::MemoryBufferIdPtr& input)
    {
        return input->get_mailbox();
    }

    static const ::viz::SharedBitmapId& shared_bitmap_id(const ::viz::mojom::MemoryBufferIdPtr& input)
    {
        return input->get_shared_bitmap_id();
    }

    static bool Read(::viz::mojom::MemoryBufferId::DataView input, ::viz::mojom::MemoryBufferIdPtr* output);
};

} // namespace mojo

#endif // SERVICES_VIZ_PUBLIC_MOJOM_COMPOSITING_TRANSFERABLE_RESOURCE_MOJOM_H_
