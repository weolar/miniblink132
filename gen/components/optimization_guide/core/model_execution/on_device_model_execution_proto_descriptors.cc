// DO NOT MODIFY. GENERATED BY gen_on_device_proto_descriptors.py

#include "components/optimization_guide/core/model_execution/on_device_model_execution_proto_descriptors.h"
#include "components/optimization_guide/core/optimization_guide_util.h"

#include "base/values.h"
#include "components/optimization_guide/proto/features/compose.pb.h"
#include "components/optimization_guide/proto/features/history_answer.pb.h"
#include "components/optimization_guide/proto/features/history_query_intent.pb.h"
#include "components/optimization_guide/proto/features/prompt_api.pb.h"
#include "components/optimization_guide/proto/features/summarize.pb.h"
#include "components/optimization_guide/proto/features/tab_organization.pb.h"
#include "components/optimization_guide/proto/string_value.pb.h"

namespace optimization_guide {

namespace {

std::optional<proto::Value> GetProtoValue(const google::protobuf::MessageLite& msg, const proto::ProtoField& proto_field, int32_t index)
{
    if (index >= proto_field.proto_descriptors_size()) {
        return std::nullopt;
    }
    int32_t tag_number = proto_field.proto_descriptors(index).tag_number();
    if (msg.GetTypeName() == "optimization_guide.proto.AXAttribute") {
        const optimization_guide::proto::AXAttribute& casted_msg = static_cast<const optimization_guide::proto::AXAttribute&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.string_type()));
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.int_type()));
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.float_type()));
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.bool_type()));
            return value;
        }
        case 5: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.intlist_type()));
            return value;
        }
        case 6: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.stringlist_type()));
            return value;
        }
        case 7: {
            proto::Value value;
            value.set_string_value(casted_msg.html_attribute_name());
            return value;
        }
        case 8: {
            proto::Value value;
            value.set_string_value(casted_msg.string_value());
            return value;
        }
        case 9: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.int_value()));
            return value;
        }
        case 10: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.float_value()));
            return value;
        }
        case 11: {
            proto::Value value;
            value.set_boolean_value(casted_msg.bool_value());
            return value;
        }
        case 12: {
            return GetProtoValue(casted_msg.int_list_value(), proto_field, index + 1);
        }
        case 13: {
            return GetProtoValue(casted_msg.string_list_value(), proto_field, index + 1);
        }
        case 14: {
            proto::Value value;
            value.set_string_value(casted_msg.html_attribute_value());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.AXNodeData") {
        const optimization_guide::proto::AXNodeData& casted_msg = static_cast<const optimization_guide::proto::AXNodeData&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.id()));
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.role()));
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.state()));
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_int64_value(static_cast<int64_t>(casted_msg.actions()));
            return value;
        }
        case 7: {
            return GetProtoValue(casted_msg.relative_bounds(), proto_field, index + 1);
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.AXRelativeBounds") {
        const optimization_guide::proto::AXRelativeBounds& casted_msg = static_cast<const optimization_guide::proto::AXRelativeBounds&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.offset_container_id()));
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.x()));
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.y()));
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.width()));
            return value;
        }
        case 5: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.height()));
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.AXTreeData") {
        const optimization_guide::proto::AXTreeData& casted_msg = static_cast<const optimization_guide::proto::AXTreeData&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.doctype());
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_boolean_value(casted_msg.loaded());
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.loading_progress()));
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_string_value(casted_msg.mimetype());
            return value;
        }
        case 5: {
            proto::Value value;
            value.set_string_value(casted_msg.title());
            return value;
        }
        case 6: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.focus_id()));
            return value;
        }
        case 7: {
            proto::Value value;
            value.set_boolean_value(casted_msg.sel_is_backward());
            return value;
        }
        case 8: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.sel_anchor_object_id()));
            return value;
        }
        case 9: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.sel_anchor_offset()));
            return value;
        }
        case 10: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.sel_anchor_affinity()));
            return value;
        }
        case 11: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.sel_focus_object_id()));
            return value;
        }
        case 12: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.sel_focus_offset()));
            return value;
        }
        case 13: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.sel_focus_affinity()));
            return value;
        }
        case 14: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.root_scroller_id()));
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.AXTreeUpdate") {
        const optimization_guide::proto::AXTreeUpdate& casted_msg = static_cast<const optimization_guide::proto::AXTreeUpdate&>(msg);
        switch (tag_number) {
        case 1: {
            return GetProtoValue(casted_msg.tree_data(), proto_field, index + 1);
        }
        case 2: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.root_id()));
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.Answer") {
        const optimization_guide::proto::Answer& casted_msg = static_cast<const optimization_guide::proto::Answer&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.text());
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_float_value(static_cast<double>(casted_msg.score()));
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.Citation") {
        const optimization_guide::proto::Citation& casted_msg = static_cast<const optimization_guide::proto::Citation&>(msg);
        switch (tag_number) {
        case 2: {
            proto::Value value;
            value.set_string_value(casted_msg.passage_id());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.ComposePageMetadata") {
        const optimization_guide::proto::ComposePageMetadata& casted_msg = static_cast<const optimization_guide::proto::ComposePageMetadata&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.page_url());
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_string_value(casted_msg.page_title());
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_string_value(casted_msg.page_inner_text());
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_int64_value(static_cast<int64_t>(casted_msg.page_inner_text_offset()));
            return value;
        }
        case 5: {
            proto::Value value;
            value.set_string_value(casted_msg.trimmed_page_inner_text());
            return value;
        }
        case 6: {
            return GetProtoValue(casted_msg.ax_tree_update(), proto_field, index + 1);
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.ComposeRequest") {
        const optimization_guide::proto::ComposeRequest& casted_msg = static_cast<const optimization_guide::proto::ComposeRequest&>(msg);
        switch (tag_number) {
        case 7: {
            return GetProtoValue(casted_msg.generate_params(), proto_field, index + 1);
        }
        case 8: {
            return GetProtoValue(casted_msg.rewrite_params(), proto_field, index + 1);
        }
        case 3: {
            return GetProtoValue(casted_msg.page_metadata(), proto_field, index + 1);
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.ComposeRequest.GenerateParams") {
        const optimization_guide::proto::ComposeRequest_GenerateParams& casted_msg
            = static_cast<const optimization_guide::proto::ComposeRequest_GenerateParams&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.user_input());
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.upfront_input_mode()));
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.ComposeRequest.RewriteParams") {
        const optimization_guide::proto::ComposeRequest_RewriteParams& casted_msg
            = static_cast<const optimization_guide::proto::ComposeRequest_RewriteParams&>(msg);
        switch (tag_number) {
        case 2: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.tone()));
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.length()));
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_boolean_value(casted_msg.regenerate());
            return value;
        }
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.previous_response());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.ComposeResponse") {
        const optimization_guide::proto::ComposeResponse& casted_msg = static_cast<const optimization_guide::proto::ComposeResponse&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.output());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.HistoryAnswerRequest") {
        const optimization_guide::proto::HistoryAnswerRequest& casted_msg = static_cast<const optimization_guide::proto::HistoryAnswerRequest&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.query());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.HistoryAnswerResponse") {
        const optimization_guide::proto::HistoryAnswerResponse& casted_msg = static_cast<const optimization_guide::proto::HistoryAnswerResponse&>(msg);
        switch (tag_number) {
        case 1: {
            return GetProtoValue(casted_msg.answer(), proto_field, index + 1);
        }
        case 2: {
            proto::Value value;
            value.set_boolean_value(casted_msg.is_unanswerable());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.HistoryQueryIntentRequest") {
        const optimization_guide::proto::HistoryQueryIntentRequest& casted_msg = static_cast<const optimization_guide::proto::HistoryQueryIntentRequest&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.text());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.HistoryQueryIntentResponse") {
        const optimization_guide::proto::HistoryQueryIntentResponse& casted_msg
            = static_cast<const optimization_guide::proto::HistoryQueryIntentResponse&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_boolean_value(casted_msg.is_answer_seeking());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.PageContext") {
        const optimization_guide::proto::PageContext& casted_msg = static_cast<const optimization_guide::proto::PageContext&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.url());
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_string_value(casted_msg.title());
            return value;
        }
        case 3: {
            return GetProtoValue(casted_msg.ax_tree_data(), proto_field, index + 1);
        }
        case 4: {
            proto::Value value;
            value.set_string_value(casted_msg.inner_text());
            return value;
        }
        case 5: {
            proto::Value value;
            value.set_int64_value(static_cast<int64_t>(casted_msg.inner_text_offset()));
            return value;
        }
        case 6: {
            proto::Value value;
            value.set_string_value(casted_msg.tab_screenshot());
            return value;
        }
        case 7: {
            proto::Value value;
            value.set_string_value(casted_msg.pdf_data());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.Passage") {
        const optimization_guide::proto::Passage& casted_msg = static_cast<const optimization_guide::proto::Passage&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.text());
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_string_value(casted_msg.passage_id());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.PromptApiPrompt") {
        const optimization_guide::proto::PromptApiPrompt& casted_msg = static_cast<const optimization_guide::proto::PromptApiPrompt&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.role()));
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_string_value(casted_msg.content());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.StringValue") {
        const optimization_guide::proto::StringValue& casted_msg = static_cast<const optimization_guide::proto::StringValue&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.value());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.SummarizeOptions") {
        const optimization_guide::proto::SummarizeOptions& casted_msg = static_cast<const optimization_guide::proto::SummarizeOptions&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.output_type()));
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.output_format()));
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.output_length()));
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.SummarizeRequest") {
        const optimization_guide::proto::SummarizeRequest& casted_msg = static_cast<const optimization_guide::proto::SummarizeRequest&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.article());
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_string_value(casted_msg.context());
            return value;
        }
        case 3: {
            return GetProtoValue(casted_msg.options(), proto_field, index + 1);
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.Tab") {
        const optimization_guide::proto::Tab& casted_msg = static_cast<const optimization_guide::proto::Tab&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_int64_value(static_cast<int64_t>(casted_msg.tab_id()));
            return value;
        }
        case 2: {
            proto::Value value;
            value.set_string_value(casted_msg.title());
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_string_value(casted_msg.url());
            return value;
        }
        case 4: {
            return GetProtoValue(casted_msg.page_context(), proto_field, index + 1);
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.TabGroup") {
        const optimization_guide::proto::TabGroup& casted_msg = static_cast<const optimization_guide::proto::TabGroup&>(msg);
        switch (tag_number) {
        case 1: {
            proto::Value value;
            value.set_string_value(casted_msg.label());
            return value;
        }
        case 3: {
            proto::Value value;
            value.set_string_value(casted_msg.group_id());
            return value;
        }
        }
    }

    if (msg.GetTypeName() == "optimization_guide.proto.TabOrganizationRequest") {
        const optimization_guide::proto::TabOrganizationRequest& casted_msg = static_cast<const optimization_guide::proto::TabOrganizationRequest&>(msg);
        switch (tag_number) {
        case 2: {
            proto::Value value;
            value.set_int64_value(static_cast<int64_t>(casted_msg.active_tab_id()));
            return value;
        }
        case 4: {
            proto::Value value;
            value.set_boolean_value(casted_msg.allow_reorganizing_existing_groups());
            return value;
        }
        case 5: {
            proto::Value value;
            value.set_int32_value(static_cast<int32_t>(casted_msg.model_strategy()));
            return value;
        }
        }
    }

    return std::nullopt;
}

std::optional<NestedMessageIterator> GetProtoRepeated(const google::protobuf::MessageLite* msg, const proto::ProtoField& proto_field, int32_t index)
{
    if (index >= proto_field.proto_descriptors_size()) {
        return std::nullopt;
    }
    int32_t tag_number = proto_field.proto_descriptors(index).tag_number();
    if (msg->GetTypeName() == "optimization_guide.proto.AXAttribute") {
        switch (tag_number) {
        case 12: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::AXAttribute*>(msg)->int_list_value(), proto_field, index + 1);
        }
        case 13: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::AXAttribute*>(msg)->string_list_value(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.AXIntList") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.AXNodeData") {
        switch (tag_number) {
        case 5: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::AXNodeData*>(msg)->attributes().size(), 0);
        }
        case 7: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::AXNodeData*>(msg)->relative_bounds(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.AXRelativeBounds") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.AXStringList") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.AXTreeData") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.AXTreeUpdate") {
        switch (tag_number) {
        case 1: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::AXTreeUpdate*>(msg)->tree_data(), proto_field, index + 1);
        }
        case 3: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::AXTreeUpdate*>(msg)->nodes().size(), 0);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.Answer") {
        switch (tag_number) {
        case 3: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::Answer*>(msg)->citations().size(), 0);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.Citation") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.ComposePageMetadata") {
        switch (tag_number) {
        case 6: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::ComposePageMetadata*>(msg)->ax_tree_update(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.ComposeRequest") {
        switch (tag_number) {
        case 7: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::ComposeRequest*>(msg)->generate_params(), proto_field, index + 1);
        }
        case 8: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::ComposeRequest*>(msg)->rewrite_params(), proto_field, index + 1);
        }
        case 3: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::ComposeRequest*>(msg)->page_metadata(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.ComposeRequest.GenerateParams") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.ComposeRequest.RewriteParams") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.ComposeResponse") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.HistoryAnswerRequest") {
        switch (tag_number) {
        case 2: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::HistoryAnswerRequest*>(msg)->passages().size(), 0);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.HistoryAnswerResponse") {
        switch (tag_number) {
        case 1: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::HistoryAnswerResponse*>(msg)->answer(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.HistoryQueryIntentRequest") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.HistoryQueryIntentResponse") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.PageContext") {
        switch (tag_number) {
        case 3: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::PageContext*>(msg)->ax_tree_data(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.Passage") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.PromptApiPrompt") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.PromptApiRequest") {
        switch (tag_number) {
        case 1: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::PromptApiRequest*>(msg)->initial_prompts().size(), 0);
        }
        case 2: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::PromptApiRequest*>(msg)->prompt_history().size(), 0);
        }
        case 3: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::PromptApiRequest*>(msg)->current_prompts().size(), 0);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.StringValue") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.SummarizeOptions") {
        switch (tag_number) {
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.SummarizeRequest") {
        switch (tag_number) {
        case 3: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::SummarizeRequest*>(msg)->options(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.Tab") {
        switch (tag_number) {
        case 4: {
            return GetProtoRepeated(&static_cast<const optimization_guide::proto::Tab*>(msg)->page_context(), proto_field, index + 1);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.TabGroup") {
        switch (tag_number) {
        case 2: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::TabGroup*>(msg)->tabs().size(), 0);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.TabOrganizationRequest") {
        switch (tag_number) {
        case 1: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::TabOrganizationRequest*>(msg)->tabs().size(), 0);
        }
        case 3: {
            return NestedMessageIterator(
                msg, tag_number, static_cast<const optimization_guide::proto::TabOrganizationRequest*>(msg)->pre_existing_tab_groups().size(), 0);
        }
        }
    }

    if (msg->GetTypeName() == "optimization_guide.proto.TabOrganizationResponse") {
        switch (tag_number) {
        case 1: {
            return NestedMessageIterator(msg, tag_number, static_cast<const optimization_guide::proto::TabOrganizationResponse*>(msg)->tab_groups().size(), 0);
        }
        }
    }

    return std::nullopt;
}

std::optional<proto::Any> SetProtoValue(const std::string& proto_name, const proto::ProtoField& proto_field, const std::string& value, int32_t index)
{
    if (index >= proto_field.proto_descriptors_size()) {
        return std::nullopt;
    }
    if (proto_name == "optimization_guide.proto.AXAttribute") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 7: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.AXAttribute");
            optimization_guide::proto::AXAttribute response_value;
            response_value.set_html_attribute_name(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 8: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.AXAttribute");
            optimization_guide::proto::AXAttribute response_value;
            response_value.set_string_value(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 14: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.AXAttribute");
            optimization_guide::proto::AXAttribute response_value;
            response_value.set_html_attribute_value(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.AXIntList") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.AXNodeData") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.AXRelativeBounds") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.AXStringList") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.AXTreeData") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 1: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.AXTreeData");
            optimization_guide::proto::AXTreeData response_value;
            response_value.set_doctype(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 4: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.AXTreeData");
            optimization_guide::proto::AXTreeData response_value;
            response_value.set_mimetype(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 5: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.AXTreeData");
            optimization_guide::proto::AXTreeData response_value;
            response_value.set_title(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.AXTreeUpdate") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.Answer") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 1: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.Answer");
            optimization_guide::proto::Answer response_value;
            response_value.set_text(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.Citation") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 2: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.Citation");
            optimization_guide::proto::Citation response_value;
            response_value.set_passage_id(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.ComposeResponse") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 1: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.ComposeResponse");
            optimization_guide::proto::ComposeResponse response_value;
            response_value.set_output(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.HistoryAnswerResponse") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.HistoryQueryIntentResponse") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.PageContext") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 1: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.PageContext");
            optimization_guide::proto::PageContext response_value;
            response_value.set_url(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 2: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.PageContext");
            optimization_guide::proto::PageContext response_value;
            response_value.set_title(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 4: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.PageContext");
            optimization_guide::proto::PageContext response_value;
            response_value.set_inner_text(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 6: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.PageContext");
            optimization_guide::proto::PageContext response_value;
            response_value.set_tab_screenshot(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 7: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.PageContext");
            optimization_guide::proto::PageContext response_value;
            response_value.set_pdf_data(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.StringValue") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 1: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.StringValue");
            optimization_guide::proto::StringValue response_value;
            response_value.set_value(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.Tab") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 2: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.Tab");
            optimization_guide::proto::Tab response_value;
            response_value.set_title(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 3: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.Tab");
            optimization_guide::proto::Tab response_value;
            response_value.set_url(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.TabGroup") {
        switch (proto_field.proto_descriptors(index).tag_number()) {
        case 1: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.TabGroup");
            optimization_guide::proto::TabGroup response_value;
            response_value.set_label(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }
        case 3: {
            proto::Any any;
            any.set_type_url("type.googleapis.com/optimization_guide.proto.TabGroup");
            optimization_guide::proto::TabGroup response_value;
            response_value.set_group_id(value);
            response_value.SerializeToString(any.mutable_value());
            return any;
        }

        default:
            return std::nullopt;
        }
    }
    if (proto_name == "optimization_guide.proto.TabOrganizationResponse") {
        switch (proto_field.proto_descriptors(index).tag_number()) {

        default:
            return std::nullopt;
        }
    }

    return std::nullopt;
}

bool Convert_optimization_guide_proto_AXAttribute(const base::Value& object, optimization_guide::proto::AXAttribute& proto);

bool Convert_optimization_guide_proto_AXIntList(const base::Value& object, optimization_guide::proto::AXIntList& proto);

bool Convert_optimization_guide_proto_AXNodeData(const base::Value& object, optimization_guide::proto::AXNodeData& proto);

bool Convert_optimization_guide_proto_AXRelativeBounds(const base::Value& object, optimization_guide::proto::AXRelativeBounds& proto);

bool Convert_optimization_guide_proto_AXStringList(const base::Value& object, optimization_guide::proto::AXStringList& proto);

bool Convert_optimization_guide_proto_AXTreeData(const base::Value& object, optimization_guide::proto::AXTreeData& proto);

bool Convert_optimization_guide_proto_AXTreeUpdate(const base::Value& object, optimization_guide::proto::AXTreeUpdate& proto);

bool Convert_optimization_guide_proto_Answer(const base::Value& object, optimization_guide::proto::Answer& proto);

bool Convert_optimization_guide_proto_Citation(const base::Value& object, optimization_guide::proto::Citation& proto);

bool Convert_optimization_guide_proto_ComposeResponse(const base::Value& object, optimization_guide::proto::ComposeResponse& proto);

bool Convert_optimization_guide_proto_HistoryAnswerResponse(const base::Value& object, optimization_guide::proto::HistoryAnswerResponse& proto);

bool Convert_optimization_guide_proto_HistoryQueryIntentResponse(const base::Value& object, optimization_guide::proto::HistoryQueryIntentResponse& proto);

bool Convert_optimization_guide_proto_PageContext(const base::Value& object, optimization_guide::proto::PageContext& proto);

bool Convert_optimization_guide_proto_StringValue(const base::Value& object, optimization_guide::proto::StringValue& proto);

bool Convert_optimization_guide_proto_Tab(const base::Value& object, optimization_guide::proto::Tab& proto);

bool Convert_optimization_guide_proto_TabGroup(const base::Value& object, optimization_guide::proto::TabGroup& proto);

bool Convert_optimization_guide_proto_TabOrganizationResponse(const base::Value& object, optimization_guide::proto::TabOrganizationResponse& proto);

bool Convert_optimization_guide_proto_AXAttribute(const base::Value& object, optimization_guide::proto::AXAttribute& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("htmlAttributeName")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_html_attribute_name(*v);
    }
    if (const base::Value* field_value = asdict->Find("stringValue")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_string_value(*v);
    }
    if (const base::Value* field_value = asdict->Find("intValue")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_int_value(*v);
    }
    if (const base::Value* field_value = asdict->Find("floatValue")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_float_value(*v);
    }
    if (const base::Value* field_value = asdict->Find("boolValue")) {
        const std::optional<bool> v = field_value->GetIfBool();
        if (!v) {
            return false;
        }
        proto.set_bool_value(*v);
    }
    if (const base::Value* field_value = asdict->Find("intListValue")) {
        if (!Convert_optimization_guide_proto_AXIntList(*field_value, *proto.mutable_int_list_value())) {
            return false;
        }
    }
    if (const base::Value* field_value = asdict->Find("stringListValue")) {
        if (!Convert_optimization_guide_proto_AXStringList(*field_value, *proto.mutable_string_list_value())) {
            return false;
        }
    }
    if (const base::Value* field_value = asdict->Find("htmlAttributeValue")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_html_attribute_value(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_AXIntList(const base::Value& object, optimization_guide::proto::AXIntList& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("value")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            const std::optional<int> v = entry_value.GetIfInt();
            if (!v) {
                return false;
            }
            proto.add_value(*v);
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_AXNodeData(const base::Value& object, optimization_guide::proto::AXNodeData& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("id")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("state")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_state(*v);
    }
    if (const base::Value* field_value = asdict->Find("actions")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_actions(*v);
    }
    if (const base::Value* field_value = asdict->Find("attributes")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            if (!Convert_optimization_guide_proto_AXAttribute(entry_value, *proto.add_attributes())) {
                return false;
            }
        }
    }
    if (const base::Value* field_value = asdict->Find("childIds")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            const std::optional<int> v = entry_value.GetIfInt();
            if (!v) {
                return false;
            }
            proto.add_child_ids(*v);
        }
    }
    if (const base::Value* field_value = asdict->Find("relativeBounds")) {
        if (!Convert_optimization_guide_proto_AXRelativeBounds(*field_value, *proto.mutable_relative_bounds())) {
            return false;
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_AXRelativeBounds(const base::Value& object, optimization_guide::proto::AXRelativeBounds& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("offsetContainerId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_offset_container_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("x")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_x(*v);
    }
    if (const base::Value* field_value = asdict->Find("y")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_y(*v);
    }
    if (const base::Value* field_value = asdict->Find("width")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_width(*v);
    }
    if (const base::Value* field_value = asdict->Find("height")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_height(*v);
    }
    if (const base::Value* field_value = asdict->Find("transform")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            const std::optional<double> v = entry_value.GetIfDouble();
            if (!v) {
                return false;
            }
            proto.add_transform(*v);
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_AXStringList(const base::Value& object, optimization_guide::proto::AXStringList& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("value")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            const std::string* v = entry_value.GetIfString();
            if (!v) {
                return false;
            }
            proto.add_value(*v);
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_AXTreeData(const base::Value& object, optimization_guide::proto::AXTreeData& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("doctype")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_doctype(*v);
    }
    if (const base::Value* field_value = asdict->Find("loaded")) {
        const std::optional<bool> v = field_value->GetIfBool();
        if (!v) {
            return false;
        }
        proto.set_loaded(*v);
    }
    if (const base::Value* field_value = asdict->Find("loadingProgress")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_loading_progress(*v);
    }
    if (const base::Value* field_value = asdict->Find("mimetype")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_mimetype(*v);
    }
    if (const base::Value* field_value = asdict->Find("title")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_title(*v);
    }
    if (const base::Value* field_value = asdict->Find("focusId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_focus_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("selIsBackward")) {
        const std::optional<bool> v = field_value->GetIfBool();
        if (!v) {
            return false;
        }
        proto.set_sel_is_backward(*v);
    }
    if (const base::Value* field_value = asdict->Find("selAnchorObjectId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_sel_anchor_object_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("selAnchorOffset")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_sel_anchor_offset(*v);
    }
    if (const base::Value* field_value = asdict->Find("selFocusObjectId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_sel_focus_object_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("selFocusOffset")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_sel_focus_offset(*v);
    }
    if (const base::Value* field_value = asdict->Find("rootScrollerId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_root_scroller_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("metadata")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            const std::string* v = entry_value.GetIfString();
            if (!v) {
                return false;
            }
            proto.add_metadata(*v);
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_AXTreeUpdate(const base::Value& object, optimization_guide::proto::AXTreeUpdate& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("treeData")) {
        if (!Convert_optimization_guide_proto_AXTreeData(*field_value, *proto.mutable_tree_data())) {
            return false;
        }
    }
    if (const base::Value* field_value = asdict->Find("rootId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_root_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("nodes")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            if (!Convert_optimization_guide_proto_AXNodeData(entry_value, *proto.add_nodes())) {
                return false;
            }
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_Answer(const base::Value& object, optimization_guide::proto::Answer& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("text")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_text(*v);
    }
    if (const base::Value* field_value = asdict->Find("score")) {
        const std::optional<double> v = field_value->GetIfDouble();
        if (!v) {
            return false;
        }
        proto.set_score(*v);
    }
    if (const base::Value* field_value = asdict->Find("citations")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            if (!Convert_optimization_guide_proto_Citation(entry_value, *proto.add_citations())) {
                return false;
            }
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_Citation(const base::Value& object, optimization_guide::proto::Citation& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("passageId")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_passage_id(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_ComposeResponse(const base::Value& object, optimization_guide::proto::ComposeResponse& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("output")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_output(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_HistoryAnswerResponse(const base::Value& object, optimization_guide::proto::HistoryAnswerResponse& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("answer")) {
        if (!Convert_optimization_guide_proto_Answer(*field_value, *proto.mutable_answer())) {
            return false;
        }
    }
    if (const base::Value* field_value = asdict->Find("isUnanswerable")) {
        const std::optional<bool> v = field_value->GetIfBool();
        if (!v) {
            return false;
        }
        proto.set_is_unanswerable(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_HistoryQueryIntentResponse(const base::Value& object, optimization_guide::proto::HistoryQueryIntentResponse& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("isAnswerSeeking")) {
        const std::optional<bool> v = field_value->GetIfBool();
        if (!v) {
            return false;
        }
        proto.set_is_answer_seeking(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_PageContext(const base::Value& object, optimization_guide::proto::PageContext& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("url")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_url(*v);
    }
    if (const base::Value* field_value = asdict->Find("title")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_title(*v);
    }
    if (const base::Value* field_value = asdict->Find("axTreeData")) {
        if (!Convert_optimization_guide_proto_AXTreeUpdate(*field_value, *proto.mutable_ax_tree_data())) {
            return false;
        }
    }
    if (const base::Value* field_value = asdict->Find("innerText")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_inner_text(*v);
    }
    if (const base::Value* field_value = asdict->Find("innerTextOffset")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_inner_text_offset(*v);
    }
    if (const base::Value* field_value = asdict->Find("tabScreenshot")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_tab_screenshot(*v);
    }
    if (const base::Value* field_value = asdict->Find("pdfData")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_pdf_data(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_StringValue(const base::Value& object, optimization_guide::proto::StringValue& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("value")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_value(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_Tab(const base::Value& object, optimization_guide::proto::Tab& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("tabId")) {
        const std::optional<int> v = field_value->GetIfInt();
        if (!v) {
            return false;
        }
        proto.set_tab_id(*v);
    }
    if (const base::Value* field_value = asdict->Find("title")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_title(*v);
    }
    if (const base::Value* field_value = asdict->Find("url")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_url(*v);
    }
    if (const base::Value* field_value = asdict->Find("pageContext")) {
        if (!Convert_optimization_guide_proto_PageContext(*field_value, *proto.mutable_page_context())) {
            return false;
        }
    }
    return true;
}

bool Convert_optimization_guide_proto_TabGroup(const base::Value& object, optimization_guide::proto::TabGroup& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("label")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_label(*v);
    }
    if (const base::Value* field_value = asdict->Find("tabs")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            if (!Convert_optimization_guide_proto_Tab(entry_value, *proto.add_tabs())) {
                return false;
            }
        }
    }
    if (const base::Value* field_value = asdict->Find("groupId")) {
        const std::string* v = field_value->GetIfString();
        if (!v) {
            return false;
        }
        proto.set_group_id(*v);
    }
    return true;
}

bool Convert_optimization_guide_proto_TabOrganizationResponse(const base::Value& object, optimization_guide::proto::TabOrganizationResponse& proto)
{
    const base::Value::Dict* asdict = object.GetIfDict();
    if (!asdict) {
        return false;
    }
    if (const base::Value* field_value = asdict->Find("tabGroups")) {
        const auto* lst = field_value->GetIfList();
        if (!lst) {
            return false;
        }
        for (const base::Value& entry_value : *lst) {

            if (!Convert_optimization_guide_proto_TabGroup(entry_value, *proto.add_tab_groups())) {
                return false;
            }
        }
    }
    return true;
}
} // namespace

std::optional<proto::Value> GetProtoValue(const google::protobuf::MessageLite& msg, const proto::ProtoField& proto_field)
{
    return GetProtoValue(msg, proto_field, /*index=*/0);
}

std::optional<NestedMessageIterator> GetProtoRepeated(const google::protobuf::MessageLite* msg, const proto::ProtoField& proto_field)
{
    return GetProtoRepeated(msg, proto_field, /*index=*/0);
}

std::unique_ptr<google::protobuf::MessageLite> GetProtoFromAny(const proto::Any& msg)
{
    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXAttribute") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXAttribute>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXIntList") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXIntList>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXNodeData") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXNodeData>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXRelativeBounds") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXRelativeBounds>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXStringList") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXStringList>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXTreeData") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXTreeData>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.AXTreeUpdate") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::AXTreeUpdate>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.Answer") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::Answer>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.Citation") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::Citation>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.ComposePageMetadata") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::ComposePageMetadata>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.ComposeRequest") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::ComposeRequest>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.ComposeRequest.GenerateParams") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::ComposeRequest_GenerateParams>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.ComposeRequest.RewriteParams") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::ComposeRequest_RewriteParams>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.ComposeResponse") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::ComposeResponse>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.HistoryAnswerRequest") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::HistoryAnswerRequest>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.HistoryAnswerResponse") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::HistoryAnswerResponse>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.HistoryQueryIntentRequest") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::HistoryQueryIntentRequest>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.HistoryQueryIntentResponse") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::HistoryQueryIntentResponse>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.PageContext") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::PageContext>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.Passage") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::Passage>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.PromptApiPrompt") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::PromptApiPrompt>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.PromptApiRequest") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::PromptApiRequest>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.StringValue") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::StringValue>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.SummarizeOptions") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::SummarizeOptions>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.SummarizeRequest") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::SummarizeRequest>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.Tab") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::Tab>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.TabGroup") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::TabGroup>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.TabOrganizationRequest") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::TabOrganizationRequest>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    if (msg.type_url() == "type.googleapis.com/optimization_guide.proto.TabOrganizationResponse") {
        auto casted_msg = ParsedAnyMetadata<optimization_guide::proto::TabOrganizationResponse>(msg);

        std::unique_ptr<google::protobuf::MessageLite> copy(casted_msg->New());

        copy->CheckTypeAndMergeFrom(*casted_msg);
        return copy;
    }

    return nullptr;
}

std::optional<proto::Any> SetProtoValue(const std::string& proto_name, const proto::ProtoField& proto_field, const std::string& value)
{
    return SetProtoValue(proto_name, proto_field, value, /*index=*/0);
}
const google::protobuf::MessageLite* NestedMessageIterator::Get() const
{
    if (parent_->GetTypeName() == "optimization_guide.proto.AXAttribute") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.AXIntList") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.AXNodeData") {
        switch (tag_number_) {
        case 5: {
            return &static_cast<const optimization_guide::proto::AXNodeData*>(parent_)->attributes(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.AXRelativeBounds") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.AXStringList") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.AXTreeData") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.AXTreeUpdate") {
        switch (tag_number_) {
        case 3: {
            return &static_cast<const optimization_guide::proto::AXTreeUpdate*>(parent_)->nodes(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.Answer") {
        switch (tag_number_) {
        case 3: {
            return &static_cast<const optimization_guide::proto::Answer*>(parent_)->citations(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.Citation") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.ComposePageMetadata") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.ComposeRequest") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.ComposeRequest.GenerateParams") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.ComposeRequest.RewriteParams") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.ComposeResponse") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.HistoryAnswerRequest") {
        switch (tag_number_) {
        case 2: {
            return &static_cast<const optimization_guide::proto::HistoryAnswerRequest*>(parent_)->passages(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.HistoryAnswerResponse") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.HistoryQueryIntentRequest") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.HistoryQueryIntentResponse") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.PageContext") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.Passage") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.PromptApiPrompt") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.PromptApiRequest") {
        switch (tag_number_) {
        case 1: {
            return &static_cast<const optimization_guide::proto::PromptApiRequest*>(parent_)->initial_prompts(offset_);
        }
        case 2: {
            return &static_cast<const optimization_guide::proto::PromptApiRequest*>(parent_)->prompt_history(offset_);
        }
        case 3: {
            return &static_cast<const optimization_guide::proto::PromptApiRequest*>(parent_)->current_prompts(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.StringValue") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.SummarizeOptions") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.SummarizeRequest") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.Tab") {
        switch (tag_number_) {
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.TabGroup") {
        switch (tag_number_) {
        case 2: {
            return &static_cast<const optimization_guide::proto::TabGroup*>(parent_)->tabs(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.TabOrganizationRequest") {
        switch (tag_number_) {
        case 1: {
            return &static_cast<const optimization_guide::proto::TabOrganizationRequest*>(parent_)->tabs(offset_);
        }
        case 3: {
            return &static_cast<const optimization_guide::proto::TabOrganizationRequest*>(parent_)->pre_existing_tab_groups(offset_);
        }
        }
    }

    if (parent_->GetTypeName() == "optimization_guide.proto.TabOrganizationResponse") {
        switch (tag_number_) {
        case 1: {
            return &static_cast<const optimization_guide::proto::TabOrganizationResponse*>(parent_)->tab_groups(offset_);
        }
        }
    }

    NOTREACHED();
}

std::optional<proto::Any> ConvertToAnyWrappedProto(const base::Value& object, const std::string& type_name)
{
    proto::Any any;
    any.set_type_url("type.googleapis.com/" + type_name);

    if (type_name == "optimization_guide.proto.AXAttribute") {
        optimization_guide::proto::AXAttribute msg;
        if (Convert_optimization_guide_proto_AXAttribute(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.AXIntList") {
        optimization_guide::proto::AXIntList msg;
        if (Convert_optimization_guide_proto_AXIntList(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.AXNodeData") {
        optimization_guide::proto::AXNodeData msg;
        if (Convert_optimization_guide_proto_AXNodeData(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.AXRelativeBounds") {
        optimization_guide::proto::AXRelativeBounds msg;
        if (Convert_optimization_guide_proto_AXRelativeBounds(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.AXStringList") {
        optimization_guide::proto::AXStringList msg;
        if (Convert_optimization_guide_proto_AXStringList(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.AXTreeData") {
        optimization_guide::proto::AXTreeData msg;
        if (Convert_optimization_guide_proto_AXTreeData(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.AXTreeUpdate") {
        optimization_guide::proto::AXTreeUpdate msg;
        if (Convert_optimization_guide_proto_AXTreeUpdate(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.Answer") {
        optimization_guide::proto::Answer msg;
        if (Convert_optimization_guide_proto_Answer(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.Citation") {
        optimization_guide::proto::Citation msg;
        if (Convert_optimization_guide_proto_Citation(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.ComposeResponse") {
        optimization_guide::proto::ComposeResponse msg;
        if (Convert_optimization_guide_proto_ComposeResponse(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.HistoryAnswerResponse") {
        optimization_guide::proto::HistoryAnswerResponse msg;
        if (Convert_optimization_guide_proto_HistoryAnswerResponse(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.HistoryQueryIntentResponse") {
        optimization_guide::proto::HistoryQueryIntentResponse msg;
        if (Convert_optimization_guide_proto_HistoryQueryIntentResponse(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.PageContext") {
        optimization_guide::proto::PageContext msg;
        if (Convert_optimization_guide_proto_PageContext(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.StringValue") {
        optimization_guide::proto::StringValue msg;
        if (Convert_optimization_guide_proto_StringValue(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.Tab") {
        optimization_guide::proto::Tab msg;
        if (Convert_optimization_guide_proto_Tab(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.TabGroup") {
        optimization_guide::proto::TabGroup msg;
        if (Convert_optimization_guide_proto_TabGroup(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    if (type_name == "optimization_guide.proto.TabOrganizationResponse") {
        optimization_guide::proto::TabOrganizationResponse msg;
        if (Convert_optimization_guide_proto_TabOrganizationResponse(object, msg)) {
            msg.SerializeToString(any.mutable_value());
            return any;
        }
    }

    return std::nullopt;
}
NestedMessageIterator::NestedMessageIterator(const google::protobuf::MessageLite* parent, int32_t tag_number, int32_t field_size, int32_t offset)
    : parent_(parent)
    , tag_number_(tag_number)
    , field_size_(field_size)
    , offset_(offset)
{
}
} // namespace optimization_guide
