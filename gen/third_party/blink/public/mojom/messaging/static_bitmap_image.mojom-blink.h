// third_party/blink/public/mojom/messaging/static_bitmap_image.mojom-blink.h is auto generated by mojom_bindings_generator.py, do not edit

// Copyright 2013 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

#ifndef THIRD_PARTY_BLINK_PUBLIC_MOJOM_MESSAGING_STATIC_BITMAP_IMAGE_MOJOM_BLINK_H_
#define THIRD_PARTY_BLINK_PUBLIC_MOJOM_MESSAGING_STATIC_BITMAP_IMAGE_MOJOM_BLINK_H_

#include <stdint.h>

#include <limits>
#include <optional>
#include <type_traits>
#include <utility>

#include "base/types/cxx23_to_underlying.h"
#include "mojo/public/cpp/bindings/clone_traits.h"
#include "mojo/public/cpp/bindings/equals_traits.h"
#include "mojo/public/cpp/bindings/lib/serialization.h"
#include "mojo/public/cpp/bindings/struct_ptr.h"
#include "mojo/public/cpp/bindings/struct_traits.h"
#include "mojo/public/cpp/bindings/union_traits.h"

#include "third_party/perfetto/include/perfetto/tracing/traced_value_forward.h"

#include "third_party/blink/public/mojom/messaging/static_bitmap_image.mojom-features.h" // IWYU pragma: export
#include "third_party/blink/public/mojom/messaging/static_bitmap_image.mojom-shared.h" // IWYU pragma: export
#include "third_party/blink/public/mojom/messaging/static_bitmap_image.mojom-blink-forward.h" // IWYU pragma: export
#include "skia/public/mojom/bitmap.mojom-blink.h"
#include "gpu/ipc/common/mailbox_holder.mojom-blink.h"
#include "gpu/ipc/common/sync_token.mojom-blink.h"
#include "skia/public/mojom/image_info.mojom-blink.h"
#include "mojo/public/mojom/base/values.mojom-blink-forward.h"

#include "mojo/public/cpp/bindings/lib/wtf_clone_equals_util.h"
#include "mojo/public/cpp/bindings/lib/wtf_hash_util.h"
#include "third_party/blink/renderer/platform/wtf/hash_functions.h"
#include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"

#include "mojo/public/cpp/bindings/lib/control_message_handler.h"
#include "mojo/public/cpp/bindings/lib/message_size_estimator.h"
#include "mojo/public/cpp/bindings/raw_ptr_impl_ref_traits.h"

#include "third_party/blink/public/common/messaging/accelerated_image_info.h"
#include "third_party/blink/renderer/core/core_export.h"
#if !BLINK_MOJO_IMPL && !INSIDE_BLINK
#error "File must only be imported inside blink"
#endif

namespace blink::mojom::blink {

class ImageReleaseCallbackProxy;

template <typename ImplRefTraits> class ImageReleaseCallbackStub;

class ImageReleaseCallbackRequestValidator;

class CORE_EXPORT ImageReleaseCallback : public ImageReleaseCallbackInterfaceBase {
public:
    using IPCStableHashFunction = uint32_t (*)();

    static const char Name_[];
    static IPCStableHashFunction MessageToMethodInfo_(mojo::Message& message);
    static const char* MessageToMethodName_(mojo::Message& message);
    static constexpr uint32_t Version_ = 0;
    static constexpr bool PassesAssociatedKinds_ = false;
    static constexpr bool HasUninterruptableMethods_ = false;

    using Base_ = ImageReleaseCallbackInterfaceBase;
    using Proxy_ = ImageReleaseCallbackProxy;

    template <typename ImplRefTraits> using Stub_ = ImageReleaseCallbackStub<ImplRefTraits>;

    using RequestValidator_ = ImageReleaseCallbackRequestValidator;
    using ResponseValidator_ = mojo::PassThroughFilter;
    enum MethodMinVersions : uint32_t {
        kReleaseMinVersion = 0,
    };

// crbug.com/1340245 - this causes binary size bloat on Fuchsia, and we're OK
// with not having this data in traces there.
#if !BUILDFLAG(IS_FUCHSIA)
    struct Release_Sym {
        NOINLINE static uint32_t IPCStableHash();
    };
#endif // !BUILDFLAG(IS_FUCHSIA)
    virtual ~ImageReleaseCallback() = default;

    virtual void Release(const ::gpu::SyncToken& token) = 0;
};

//class CORE_EXPORT ImageReleaseCallbackProxy : public ImageReleaseCallback {
//public:
//    using InterfaceType = ImageReleaseCallback;
//
//    explicit ImageReleaseCallbackProxy(mojo::MessageReceiverWithResponder* receiver);
//
//    void Release(const ::gpu::SyncToken& token) final;
//
//private:
//    mojo::MessageReceiverWithResponder* receiver_;
//};
//class CORE_EXPORT ImageReleaseCallbackStubDispatch {
//public:
//    static bool Accept(ImageReleaseCallback* impl, mojo::Message* message);
//    static bool AcceptWithResponder(ImageReleaseCallback* impl, mojo::Message* message, std::unique_ptr<mojo::MessageReceiverWithStatus> responder);
//};

//template <typename ImplRefTraits = mojo::RawPtrImplRefTraits<ImageReleaseCallback>>
//class ImageReleaseCallbackStub : public mojo::MessageReceiverWithResponderStatus {
//public:
//    using ImplPointerType = typename ImplRefTraits::PointerType;
//
//    ImageReleaseCallbackStub() = default;
//    ~ImageReleaseCallbackStub() override = default;
//
//    void set_sink(ImplPointerType sink)
//    {
//        sink_ = std::move(sink);
//    }
//    ImplPointerType& sink()
//    {
//        return sink_;
//    }
//
//    bool Accept(mojo::Message* message) override
//    {
//        if (ImplRefTraits::IsNull(sink_))
//            return false;
//        return ImageReleaseCallbackStubDispatch::Accept(ImplRefTraits::GetRawPointer(&sink_), message);
//    }
//
//    bool AcceptWithResponder(mojo::Message* message, std::unique_ptr<mojo::MessageReceiverWithStatus> responder) override
//    {
//        if (ImplRefTraits::IsNull(sink_))
//            return false;
//        return ImageReleaseCallbackStubDispatch::AcceptWithResponder(ImplRefTraits::GetRawPointer(&sink_), message, std::move(responder));
//    }
//
//private:
//    ImplPointerType sink_;
//};
//class CORE_EXPORT ImageReleaseCallbackRequestValidator : public mojo::MessageReceiver {
//public:
//    bool Accept(mojo::Message* message) override;
//};

class CORE_EXPORT SharedImageUsageSet {
public:
    template <typename T> using EnableIfSame = std::enable_if_t<std::is_same<SharedImageUsageSet, T>::value>;
    using DataView = SharedImageUsageSetDataView;
    using Data_ = internal::SharedImageUsageSet_Data;

    template <typename... Args> static SharedImageUsageSetPtr New(Args&&... args)
    {
        return SharedImageUsageSetPtr(std::in_place, std::forward<Args>(args)...);
    }

    template <typename U> static SharedImageUsageSetPtr From(const U& u)
    {
        return mojo::TypeConverter<SharedImageUsageSetPtr, U>::Convert(u);
    }

    template <typename U> U To() const
    {
        return mojo::TypeConverter<U, SharedImageUsageSet>::Convert(*this);
    }

    SharedImageUsageSet()
    : usage()
{
}

    explicit SharedImageUsageSet(uint32_t usage_in)
    : usage(std::move(usage_in)){
}

    ~SharedImageUsageSet() = default;

    // Clone() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Clone() or copy
    // constructor/assignment are available for members.
    template <typename StructPtrType = SharedImageUsageSetPtr> SharedImageUsageSetPtr Clone() const;

    // Equals() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Equals() or == operator
    // are available for members.
    template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool Equals(const T& other) const;

    template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool operator==(const T& rhs) const
    {
        return Equals(rhs);
    }

    template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool operator!=(const T& rhs) const
    {
        return !operator==(rhs);
    }
    size_t Hash(size_t seed) const;
    template <typename UserType> static WTF::Vector<uint8_t> Serialize(UserType* input)
    {
        return mojo::internal::SerializeImpl<SharedImageUsageSet::DataView, WTF::Vector<uint8_t>>(input);
    }

    template <typename UserType> static mojo::Message SerializeAsMessage(UserType* input)
    {
        return mojo::internal::SerializeAsMessageImpl<SharedImageUsageSet::DataView>(input);
    }

    // The returned Message is serialized only if the message is moved
    // cross-process or cross-language. Otherwise if the message is Deserialized
    // as the same UserType |input| will just be moved to |output| in
    // DeserializeFromMessage.
    template <typename UserType> static mojo::Message WrapAsMessage(UserType input)
    {
        return mojo::Message(
            std::make_unique<internal::SharedImageUsageSet_UnserializedMessageContext<UserType, SharedImageUsageSet::DataView>>(0, 0, std::move(input)),
            MOJO_CREATE_MESSAGE_FLAG_NONE);
    }

    template <typename UserType> static bool Deserialize(const void* data, size_t data_num_bytes, UserType* output)
    {
        mojo::Message message;
        return mojo::internal::DeserializeImpl<SharedImageUsageSet::DataView>(message, data, data_num_bytes, output, Validate);
    }

    template <typename UserType> static bool Deserialize(base::span<const uint8_t> input, UserType* output)
    {
        return SharedImageUsageSet::Deserialize(input.empty() ? nullptr : input.data(), input.size(), output);
    }

    template <typename UserType> static bool DeserializeFromMessage(mojo::Message input, UserType* output)
    {
        auto context = input.TakeUnserializedContext<internal::SharedImageUsageSet_UnserializedMessageContext<UserType, SharedImageUsageSet::DataView>>();
        if (context) {
            *output = std::move(context->TakeData());
            return true;
        }
        input.SerializeIfNecessary();
        return mojo::internal::DeserializeImpl<SharedImageUsageSet::DataView>(input, input.payload(), input.payload_num_bytes(), output, Validate);
    }

    uint32_t usage;

    // Serialise this struct into a trace.
    void WriteIntoTrace(perfetto::TracedValue traced_context) const;

private:
    static bool Validate(const void* data, mojo::internal::ValidationContext* validation_context);
};

// The comparison operators are templates, so they are only instantiated if they
// are used. Thus, the bindings generator does not need to know whether
// comparison operators are available for members.
template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool operator<(const T& lhs, const T& rhs);

template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool operator<=(const T& lhs, const T& rhs)
{
    return !(rhs < lhs);
}

template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool operator>(const T& lhs, const T& rhs)
{
    return rhs < lhs;
}

template <typename T, SharedImageUsageSet::EnableIfSame<T>* = nullptr> bool operator>=(const T& lhs, const T& rhs)
{
    return !(lhs < rhs);
}

class CORE_EXPORT SerializedStaticBitmapImage {
public:
    using DataView = SerializedStaticBitmapImageDataView;
    using Data_ = internal::SerializedStaticBitmapImage_Data;
    using Tag = Data_::SerializedStaticBitmapImage_Tag;

    template <typename... Args> static SerializedStaticBitmapImagePtr New(Args&&... args)
    {
        static_assert(sizeof...(args) < 0,
            "Do not use Union::New(); to create a union of a given subtype, use "
            "New<SubType>(), not New() followed by set_<sub_type>(). To represent "
            "an empty union, mark the field or parameter as nullable in the mojom "
            "definition.");
        return nullptr;
    }
    // Construct an instance holding |bitmap|.
    static SerializedStaticBitmapImagePtr NewBitmap(const ::SkBitmap& value)
    {
        auto result = SerializedStaticBitmapImagePtr(std::in_place);
        result->set_bitmap(std::move(value));
        return result;
    }
    // Construct an instance holding |accelerated_image|.
    static SerializedStaticBitmapImagePtr NewAcceleratedImage(::blink::AcceleratedImageInfo value)
    {
        auto result = SerializedStaticBitmapImagePtr(std::in_place);
        result->set_accelerated_image(std::move(value));
        return result;
    }

    template <typename U> static SerializedStaticBitmapImagePtr From(const U& u)
    {
        return mojo::TypeConverter<SerializedStaticBitmapImagePtr, U>::Convert(u);
    }

    template <typename U> U To() const
    {
        return mojo::TypeConverter<U, SerializedStaticBitmapImage>::Convert(*this);
    }

    SerializedStaticBitmapImage()
    : tag_(Tag::kBitmap)
{
    data_.bitmap = new ::SkBitmap;
}
    ~SerializedStaticBitmapImage(){
    DestroyActive();
}
    // Delete the copy constructor and copy assignment operators because `data_`
    // contains raw pointers that must not be copied.
    SerializedStaticBitmapImage(const SerializedStaticBitmapImage& other) = delete;
    SerializedStaticBitmapImage& operator=(const SerializedStaticBitmapImage& other) = delete;

    // Clone() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Clone() or copy
    // constructor/assignment are available for members.
    template <typename UnionPtrType = SerializedStaticBitmapImagePtr> SerializedStaticBitmapImagePtr Clone() const;

    // Equals() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Equals() or == operator
    // are available for members.
    template <typename T, typename std::enable_if<std::is_same<T, SerializedStaticBitmapImage>::value>::type* = nullptr> bool Equals(const T& other) const;

    template <typename T, typename std::enable_if<std::is_same<T, SerializedStaticBitmapImage>::value>::type* = nullptr> bool operator==(const T& rhs) const
    {
        return Equals(rhs);
    }

    Tag which() const
    {
        return tag_;
    }

    bool is_bitmap() const
    {
        return tag_ == Tag::kBitmap;
    }

    ::SkBitmap& get_bitmap() const
    {
        CHECK(tag_ == Tag::kBitmap);
        return *(data_.bitmap);
    }

    void set_bitmap(const ::SkBitmap& bitmap)
    {
        if (tag_ == Tag::kBitmap) {
            *(data_.bitmap) = std::move(bitmap);
        } else {
            DestroyActive();
            tag_ = Tag::kBitmap;
            data_.bitmap = new ::SkBitmap(std::move(bitmap));
        }
    }

    bool is_accelerated_image() const
    {
        return tag_ == Tag::kAcceleratedImage;
    }

    ::blink::AcceleratedImageInfo& get_accelerated_image() const
    {
        CHECK(tag_ == Tag::kAcceleratedImage);
        return *(data_.accelerated_image);
    }

    void set_accelerated_image(::blink::AcceleratedImageInfo accelerated_image);

    template <typename UserType> static mojo::Message SerializeAsMessage(UserType* input)
    {
        return mojo::internal::SerializeAsMessageImpl<SerializedStaticBitmapImage::DataView>(input);
    }

    template <typename UserType> static bool DeserializeFromMessage(mojo::Message input, UserType* output)
    {
        return mojo::internal::DeserializeImpl<SerializedStaticBitmapImage::DataView>(input, input.payload(), input.payload_num_bytes(), output, Validate);
    }

private:
    union Union_ {
        Union_() = default;
        ~Union_() = default;
        ::SkBitmap* bitmap;
        ::blink::AcceleratedImageInfo* accelerated_image;
    };

    static bool Validate(const void* data, mojo::internal::ValidationContext* validation_context);

    void DestroyActive();
    Tag tag_;
    Union_ data_;
};

inline void SerializedStaticBitmapImage::set_accelerated_image(::blink::AcceleratedImageInfo accelerated_image)
{
    if (tag_ == Tag::kAcceleratedImage) {
        *(data_.accelerated_image) = std::move(accelerated_image);
    } else {
        DestroyActive();
        tag_ = Tag::kAcceleratedImage;
        data_.accelerated_image = new ::blink::AcceleratedImageInfo(std::move(accelerated_image));
    }
}

inline void SerializedStaticBitmapImage::DestroyActive()
{
    switch (tag_) {
        case Tag::kBitmap:
            delete data_.bitmap;
            break;
        case Tag::kAcceleratedImage:
            delete data_.accelerated_image;
            break;
    }
}

class CORE_EXPORT AcceleratedStaticBitmapImage {
public:
    template <typename T> using EnableIfSame = std::enable_if_t<std::is_same<AcceleratedStaticBitmapImage, T>::value>;
    using DataView = AcceleratedStaticBitmapImageDataView;
    using Data_ = internal::AcceleratedStaticBitmapImage_Data;

    template <typename... Args> static AcceleratedStaticBitmapImagePtr New(Args&&... args)
    {
        return AcceleratedStaticBitmapImagePtr(std::in_place, std::forward<Args>(args)...);
    }

    template <typename U> static AcceleratedStaticBitmapImagePtr From(const U& u)
    {
        return mojo::TypeConverter<AcceleratedStaticBitmapImagePtr, U>::Convert(u);
    }

    template <typename U> U To() const
    {
        return mojo::TypeConverter<U, AcceleratedStaticBitmapImage>::Convert(*this);
    }

    AcceleratedStaticBitmapImage()
    : mailbox_holder()
    , usage()
    , image_info()
    , is_origin_top_left()
    , supports_display_compositing()
    , is_overlay_candidate()
    , release_callback()
{
}

    AcceleratedStaticBitmapImage(const ::gpu::MailboxHolder& mailbox_holder_in, SharedImageUsageSetPtr usage_in,
    const ::SkImageInfo& image_info_in, bool is_origin_top_left_in, bool supports_display_compositing_in, bool is_overlay_candidate_in,
    ::mojo::PendingRemote<ImageReleaseCallback> release_callback_in)
    : mailbox_holder(std::move(mailbox_holder_in))
    , usage(std::move(usage_in))
    , image_info(std::move(image_info_in))
    , is_origin_top_left(std::move(is_origin_top_left_in))
    , supports_display_compositing(std::move(supports_display_compositing_in))
    , is_overlay_candidate(std::move(is_overlay_candidate_in))
    , release_callback(std::move(release_callback_in)){
}

    AcceleratedStaticBitmapImage(const AcceleratedStaticBitmapImage&) = delete;
    AcceleratedStaticBitmapImage& operator=(const AcceleratedStaticBitmapImage&) = delete;

    ~AcceleratedStaticBitmapImage() = default;

    // Clone() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Clone() or copy
    // constructor/assignment are available for members.
    template <typename StructPtrType = AcceleratedStaticBitmapImagePtr> AcceleratedStaticBitmapImagePtr Clone() const;

    // Equals() is a template so it is only instantiated if it is used. Thus, the
    // bindings generator does not need to know whether Equals() or == operator
    // are available for members.
    template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool Equals(const T& other) const;

    template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool operator==(const T& rhs) const
    {
        return Equals(rhs);
    }

    template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool operator!=(const T& rhs) const
    {
        return !operator==(rhs);
    }

    template <typename UserType> static mojo::Message SerializeAsMessage(UserType* input)
    {
        return mojo::internal::SerializeAsMessageImpl<AcceleratedStaticBitmapImage::DataView>(input);
    }

    // The returned Message is serialized only if the message is moved
    // cross-process or cross-language. Otherwise if the message is Deserialized
    // as the same UserType |input| will just be moved to |output| in
    // DeserializeFromMessage.
    template <typename UserType> static mojo::Message WrapAsMessage(UserType input)
    {
        return mojo::Message(
            std::make_unique<internal::AcceleratedStaticBitmapImage_UnserializedMessageContext<UserType, AcceleratedStaticBitmapImage::DataView>>(
                0, 0, std::move(input)),
            MOJO_CREATE_MESSAGE_FLAG_NONE);
    }

    template <typename UserType> static bool Deserialize(const void* data, size_t data_num_bytes, UserType* output)
    {
        mojo::Message message;
        return mojo::internal::DeserializeImpl<AcceleratedStaticBitmapImage::DataView>(message, data, data_num_bytes, output, Validate);
    }

    template <typename UserType> static bool Deserialize(base::span<const uint8_t> input, UserType* output)
    {
        return AcceleratedStaticBitmapImage::Deserialize(input.empty() ? nullptr : input.data(), input.size(), output);
    }

    template <typename UserType> static bool DeserializeFromMessage(mojo::Message input, UserType* output)
    {
        auto context = input.TakeUnserializedContext<
            internal::AcceleratedStaticBitmapImage_UnserializedMessageContext<UserType, AcceleratedStaticBitmapImage::DataView>>();
        if (context) {
            *output = std::move(context->TakeData());
            return true;
        }
        input.SerializeIfNecessary();
        return mojo::internal::DeserializeImpl<AcceleratedStaticBitmapImage::DataView>(input, input.payload(), input.payload_num_bytes(), output, Validate);
    }

    ::gpu::MailboxHolder mailbox_holder;

    SharedImageUsageSetPtr usage;

    ::SkImageInfo image_info;

    bool is_origin_top_left;

    bool supports_display_compositing;

    bool is_overlay_candidate;

    ::mojo::PendingRemote<ImageReleaseCallback> release_callback;

    // Serialise this struct into a trace.
    void WriteIntoTrace(perfetto::TracedValue traced_context) const;

private:
    static bool Validate(const void* data, mojo::internal::ValidationContext* validation_context);
};

// The comparison operators are templates, so they are only instantiated if they
// are used. Thus, the bindings generator does not need to know whether
// comparison operators are available for members.
template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool operator<(const T& lhs, const T& rhs);

template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool operator<=(const T& lhs, const T& rhs)
{
    return !(rhs < lhs);
}

template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool operator>(const T& lhs, const T& rhs)
{
    return rhs < lhs;
}

template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>* = nullptr> bool operator>=(const T& lhs, const T& rhs)
{
    return !(lhs < rhs);
}

template <typename UnionPtrType> SerializedStaticBitmapImagePtr SerializedStaticBitmapImage::Clone() const
{
    switch (tag_) {
    case Tag::kBitmap:
        return NewBitmap(mojo::Clone(*data_.bitmap));
    case Tag::kAcceleratedImage:
        return NewAcceleratedImage(mojo::Clone(*data_.accelerated_image));
    }
    return nullptr;
}

template <typename T, typename std::enable_if<std::is_same<T, SerializedStaticBitmapImage>::value>::type*>
bool SerializedStaticBitmapImage::Equals(const T& other) const
{
    if (tag_ != other.which())
        return false;

    switch (tag_) {
    case Tag::kBitmap:
        return mojo::Equals(*(data_.bitmap), *(other.data_.bitmap));
    case Tag::kAcceleratedImage:
        return mojo::Equals(*(data_.accelerated_image), *(other.data_.accelerated_image));
    }

    return false;
}
template <typename StructPtrType> SharedImageUsageSetPtr SharedImageUsageSet::Clone() const
{
    return New(mojo::Clone(usage));
}

template <typename T, SharedImageUsageSet::EnableIfSame<T>*> bool SharedImageUsageSet::Equals(const T& other_struct) const
{
    if (!mojo::Equals(this->usage, other_struct.usage))
        return false;
    return true;
}

template <typename T, SharedImageUsageSet::EnableIfSame<T>*> bool operator<(const T& lhs, const T& rhs)
{
    if (lhs.usage < rhs.usage)
        return true;
    if (rhs.usage < lhs.usage)
        return false;
    return false;
}
template <typename StructPtrType> AcceleratedStaticBitmapImagePtr AcceleratedStaticBitmapImage::Clone() const
{
    return New(mojo::Clone(mailbox_holder), mojo::Clone(usage), mojo::Clone(image_info), mojo::Clone(is_origin_top_left),
        mojo::Clone(supports_display_compositing), mojo::Clone(is_overlay_candidate), mojo::Clone(release_callback));
}

template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>*> bool AcceleratedStaticBitmapImage::Equals(const T& other_struct) const
{
    if (!mojo::Equals(this->mailbox_holder, other_struct.mailbox_holder))
        return false;
    if (!mojo::Equals(this->usage, other_struct.usage))
        return false;
    if (!mojo::Equals(this->image_info, other_struct.image_info))
        return false;
    if (!mojo::Equals(this->is_origin_top_left, other_struct.is_origin_top_left))
        return false;
    if (!mojo::Equals(this->supports_display_compositing, other_struct.supports_display_compositing))
        return false;
    if (!mojo::Equals(this->is_overlay_candidate, other_struct.is_overlay_candidate))
        return false;
    if (!mojo::Equals(this->release_callback, other_struct.release_callback))
        return false;
    return true;
}

template <typename T, AcceleratedStaticBitmapImage::EnableIfSame<T>*> bool operator<(const T& lhs, const T& rhs)
{
    if (lhs.mailbox_holder < rhs.mailbox_holder)
        return true;
    if (rhs.mailbox_holder < lhs.mailbox_holder)
        return false;
    if (lhs.usage < rhs.usage)
        return true;
    if (rhs.usage < lhs.usage)
        return false;
    if (lhs.image_info < rhs.image_info)
        return true;
    if (rhs.image_info < lhs.image_info)
        return false;
    if (lhs.is_origin_top_left < rhs.is_origin_top_left)
        return true;
    if (rhs.is_origin_top_left < lhs.is_origin_top_left)
        return false;
    if (lhs.supports_display_compositing < rhs.supports_display_compositing)
        return true;
    if (rhs.supports_display_compositing < lhs.supports_display_compositing)
        return false;
    if (lhs.is_overlay_candidate < rhs.is_overlay_candidate)
        return true;
    if (rhs.is_overlay_candidate < lhs.is_overlay_candidate)
        return false;
    if (lhs.release_callback < rhs.release_callback)
        return true;
    if (rhs.release_callback < lhs.release_callback)
        return false;
    return false;
}

} // blink::mojom::blink

namespace mojo {

template <> struct CORE_EXPORT StructTraits<::blink::mojom::blink::SharedImageUsageSet::DataView, ::blink::mojom::blink::SharedImageUsageSetPtr> {
    static bool IsNull(const ::blink::mojom::blink::SharedImageUsageSetPtr& input)
    {
        return !input;
    }
    static void SetToNull(::blink::mojom::blink::SharedImageUsageSetPtr* output)
    {
        output->reset();
    }

    static decltype(::blink::mojom::blink::SharedImageUsageSet::usage) usage(const ::blink::mojom::blink::SharedImageUsageSetPtr& input)
    {
        return input->usage;
    }

    static bool Read(::blink::mojom::blink::SharedImageUsageSet::DataView input, ::blink::mojom::blink::SharedImageUsageSetPtr* output);
};

template <>
struct CORE_EXPORT StructTraits<::blink::mojom::blink::AcceleratedStaticBitmapImage::DataView, ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr> {
    static bool IsNull(const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return !input;
    }
    static void SetToNull(::blink::mojom::blink::AcceleratedStaticBitmapImagePtr* output)
    {
        output->reset();
    }

    static const decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::mailbox_holder)& mailbox_holder(
        const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->mailbox_holder;
    }

    static const decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::usage)& usage(
        const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->usage;
    }

    static const decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::image_info)& image_info(
        const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->image_info;
    }

    static decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::is_origin_top_left) is_origin_top_left(
        const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->is_origin_top_left;
    }

    static decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::supports_display_compositing) supports_display_compositing(
        const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->supports_display_compositing;
    }

    static decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::is_overlay_candidate) is_overlay_candidate(
        const ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->is_overlay_candidate;
    }

    static decltype(::blink::mojom::blink::AcceleratedStaticBitmapImage::release_callback)& release_callback(
        ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr& input)
    {
        return input->release_callback;
    }

    static bool Read(::blink::mojom::blink::AcceleratedStaticBitmapImage::DataView input, ::blink::mojom::blink::AcceleratedStaticBitmapImagePtr* output);
};

template <>
struct CORE_EXPORT UnionTraits<::blink::mojom::blink::SerializedStaticBitmapImage::DataView, ::blink::mojom::blink::SerializedStaticBitmapImagePtr> {
    static bool IsNull(const ::blink::mojom::blink::SerializedStaticBitmapImagePtr& input)
    {
        return !input;
    }
    static void SetToNull(::blink::mojom::blink::SerializedStaticBitmapImagePtr* output)
    {
        output->reset();
    }

    static ::blink::mojom::blink::SerializedStaticBitmapImage::Tag GetTag(const ::blink::mojom::blink::SerializedStaticBitmapImagePtr& input)
    {
        return input->which();
    }

    static ::SkBitmap& bitmap(::blink::mojom::blink::SerializedStaticBitmapImagePtr& input)
    {
        return input->get_bitmap();
    }

    static ::blink::AcceleratedImageInfo& accelerated_image(::blink::mojom::blink::SerializedStaticBitmapImagePtr& input)
    {
        return input->get_accelerated_image();
    }

    static bool Read(::blink::mojom::blink::SerializedStaticBitmapImage::DataView input, ::blink::mojom::blink::SerializedStaticBitmapImagePtr* output);
};

} // namespace mojo

#endif // THIRD_PARTY_BLINK_PUBLIC_MOJOM_MESSAGING_STATIC_BITMAP_IMAGE_MOJOM_BLINK_H_
