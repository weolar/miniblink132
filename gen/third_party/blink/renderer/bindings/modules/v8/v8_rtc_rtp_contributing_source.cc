// Copyright 2019 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// DO NOT EDIT: This file is auto-generated by
// //third_party/blink/renderer/bindings/scripts/generate_bindings.py
//
// Use the GN flag `blink_enable_generated_code_formatting=true` to enable
// formatting of the generated files.

#include "third_party/blink/renderer/bindings/modules/v8/v8_rtc_rtp_contributing_source.h"

#include "third_party/blink/renderer/bindings/core/v8/generated_code_helper.h"
#include "third_party/blink/renderer/bindings/core/v8/native_value_traits_impl.h"
#include "third_party/blink/renderer/bindings/core/v8/to_v8_traits.h"
#include "third_party/blink/renderer/platform/bindings/exception_messages.h"
#include "third_party/blink/renderer/platform/bindings/exception_state.h"
#include "third_party/blink/renderer/platform/bindings/v8_per_isolate_data.h"

namespace blink {

namespace {

const std::string_view kOwnPropertyNames[] = {
    "audioLevel",
    "captureTimestamp",
    "rtpTimestamp",
    "senderCaptureTimeOffset",
    "source",
    "timestamp",
};

} // namespace

RTCRtpContributingSource* RTCRtpContributingSource::Create(v8::Isolate* isolate, v8::Local<v8::Value> v8_value, ExceptionState& exception_state)
{
    RTCRtpContributingSource* dictionary = MakeGarbageCollected<RTCRtpContributingSource>(isolate);
    if (!v8_value->IsObject()) {
        const char* const class_like_name = "RTCRtpContributingSource";
        exception_state.ThrowTypeError(ExceptionMessages::ValueNotOfType(class_like_name));
        return nullptr;
    }
    dictionary->FillMembersFromV8Object(isolate, v8_value.As<v8::Object>(), exception_state);
    if (exception_state.HadException()) [[unlikely]] {
        return nullptr;
    }
    return dictionary;
}

RTCRtpContributingSource::RTCRtpContributingSource()
{
}

RTCRtpContributingSource::RTCRtpContributingSource(v8::Isolate* isolate)
{
}

void RTCRtpContributingSource::Trace(Visitor* visitor) const
{
    TraceIfNeeded<double>::Trace(visitor, member_audio_level_);
    TraceIfNeeded<double>::Trace(visitor, member_capture_timestamp_);
    TraceIfNeeded<uint32_t>::Trace(visitor, member_rtp_timestamp_);
    TraceIfNeeded<double>::Trace(visitor, member_sender_capture_time_offset_);
    TraceIfNeeded<uint32_t>::Trace(visitor, member_source_);
    TraceIfNeeded<double>::Trace(visitor, member_timestamp_);
    bindings::DictionaryBase::Trace(visitor);
}

void RTCRtpContributingSource::FillTemplateProperties(WTF::Vector<std::string_view>& properties) const
{
    static_assert(std::size(kOwnPropertyNames) == kOwnPropertyCount);
    properties.AppendRange(std::cbegin(kOwnPropertyNames), std::cend(kOwnPropertyNames));
    DCHECK_EQ(properties.size(), kTotalPropertyCount);
}

void RTCRtpContributingSource::FillValuesImpl(ScriptState* script_state, base::span<v8::MaybeLocal<v8::Value>> values) const
{
    CHECK_EQ(kOwnPropertyCount, values.size());
    if (hasAudioLevel()) {
        values[0] = ToV8Traits<IDLDouble>::ToV8(script_state, member_audio_level_);
        DCHECK(!values[0].IsEmpty());
    }
    if (hasCaptureTimestamp()) {
        values[1] = ToV8Traits<IDLDouble>::ToV8(script_state, member_capture_timestamp_);
        DCHECK(!values[1].IsEmpty());
    }
    if (hasRtpTimestamp()) {
        values[2] = ToV8Traits<IDLUnsignedLong>::ToV8(script_state, member_rtp_timestamp_);
        DCHECK(!values[2].IsEmpty());
    }
    if (hasSenderCaptureTimeOffset()) {
        values[3] = ToV8Traits<IDLDouble>::ToV8(script_state, member_sender_capture_time_offset_);
        DCHECK(!values[3].IsEmpty());
    }
    if (hasSource()) {
        values[4] = ToV8Traits<IDLUnsignedLong>::ToV8(script_state, member_source_);
        DCHECK(!values[4].IsEmpty());
    }
    if (hasTimestamp()) {
        values[5] = ToV8Traits<IDLDouble>::ToV8(script_state, member_timestamp_);
        DCHECK(!values[5].IsEmpty());
    }
}

const void* RTCRtpContributingSource::TemplateKey() const
{
    static const void* s_key = &s_key;
    return s_key;
}

v8::Local<v8::Object> RTCRtpContributingSource::FillValues(ScriptState* script_state, v8::Local<v8::DictionaryTemplate> dict_template) const
{
    v8::MaybeLocal<v8::Value> values[kTotalPropertyCount];
    FillValuesImpl(script_state, values);
    return dict_template->NewInstance(script_state->GetContext(), values);
}

void RTCRtpContributingSource::FillMembersFromV8Object(v8::Isolate* isolate, v8::Local<v8::Object> v8_dictionary, ExceptionState& exception_state)
{
    TryRethrowScope rethrow_scope(isolate, exception_state);
    const char* const class_like_name = "RTCRtpContributingSource";
    DictionaryConversionContext dictionary_from_v8_context(isolate, class_like_name);
    dictionary_from_v8_context.SetCurrentPropertyName("audioLevel");
    constexpr bool is_optional = false;
    v8::Local<v8::Context> current_context = isolate->GetCurrentContext();
    const auto& v8_own_member_names = GetV8OwnMemberNames(isolate);
    if (!bindings::GetDictionaryMemberFromV8Object<IDLDouble, is_optional>(isolate, current_context, v8_dictionary, v8_own_member_names[0].Get(isolate),
            has_audio_level_, member_audio_level_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("captureTimestamp");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLDouble, is_optional>(isolate, current_context, v8_dictionary, v8_own_member_names[1].Get(isolate),
            has_capture_timestamp_, member_capture_timestamp_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("rtpTimestamp");
    constexpr bool is_required = true;
    bool fallback_presence_var;
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUnsignedLong, is_required>(isolate, current_context, v8_dictionary, v8_own_member_names[2].Get(isolate),
            fallback_presence_var, member_rtp_timestamp_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("senderCaptureTimeOffset");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLDouble, is_optional>(isolate, current_context, v8_dictionary, v8_own_member_names[3].Get(isolate),
            has_sender_capture_time_offset_, member_sender_capture_time_offset_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("source");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLUnsignedLong, is_required>(isolate, current_context, v8_dictionary, v8_own_member_names[4].Get(isolate),
            fallback_presence_var, member_source_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("timestamp");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLDouble, is_required>(isolate, current_context, v8_dictionary, v8_own_member_names[5].Get(isolate),
            fallback_presence_var, member_timestamp_, class_like_name, exception_state)) {
        return;
    }
}

const base::span<const v8::Eternal<v8::Name>> RTCRtpContributingSource::GetV8OwnMemberNames(v8::Isolate* isolate)
{
    return V8PerIsolateData::From(isolate)->FindOrCreateEternalNameCache(kOwnPropertyNames, kOwnPropertyNames);
}

} // namespace blink
