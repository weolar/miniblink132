// Copyright 2019 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// DO NOT EDIT: This file is auto-generated by
// //third_party/blink/renderer/bindings/scripts/generate_bindings.py
//
// Use the GN flag `blink_enable_generated_code_formatting=true` to enable
// formatting of the generated files.

#include "third_party/blink/renderer/bindings/modules/v8/v8_rtc_ice_server.h"

#include "third_party/blink/renderer/bindings/core/v8/generated_code_helper.h"
#include "third_party/blink/renderer/bindings/core/v8/native_value_traits_impl.h"
#include "third_party/blink/renderer/bindings/core/v8/to_v8_traits.h"
#include "third_party/blink/renderer/bindings/core/v8/v8_union_string_stringsequence.h"
#include "third_party/blink/renderer/platform/bindings/exception_messages.h"
#include "third_party/blink/renderer/platform/bindings/exception_state.h"
#include "third_party/blink/renderer/platform/bindings/v8_per_isolate_data.h"

namespace blink {

namespace {

const std::string_view kOwnPropertyNames[] = {
    "credential",
    "url",
    "urls",
    "username",
};

} // namespace

RTCIceServer* RTCIceServer::Create(v8::Isolate* isolate, v8::Local<v8::Value> v8_value, ExceptionState& exception_state)
{
    RTCIceServer* dictionary = MakeGarbageCollected<RTCIceServer>(isolate);
    if (v8_value->IsNullOrUndefined()) {
        return dictionary;
    }
    if (!v8_value->IsObject()) {
        const char* const class_like_name = "RTCIceServer";
        exception_state.ThrowTypeError(ExceptionMessages::ValueNotOfType(class_like_name));
        return nullptr;
    }
    dictionary->FillMembersFromV8Object(isolate, v8_value.As<v8::Object>(), exception_state);
    if (exception_state.HadException()) [[unlikely]] {
        return nullptr;
    }
    return dictionary;
}

RTCIceServer::RTCIceServer()
{
}

RTCIceServer::RTCIceServer(v8::Isolate* isolate)
{
}

String RTCIceServer::getCredentialOr(const String& fallback_value) const
{
    if (!hasCredential()) {
        return fallback_value;
    }
    return member_credential_;
}

String RTCIceServer::getCredentialOr(String&& fallback_value) const
{
    if (!hasCredential()) {
        return std::move(fallback_value);
    }
    return member_credential_;
}

void RTCIceServer::setCredential(const String& value)
{
    member_credential_ = value;
    has_credential_ = true;
}

void RTCIceServer::setCredential(String&& value)
{
    member_credential_ = std::move(value);
    has_credential_ = true;
}

String RTCIceServer::getUrlOr(const String& fallback_value) const
{
    if (!hasUrl()) {
        return fallback_value;
    }
    return member_url_;
}

String RTCIceServer::getUrlOr(String&& fallback_value) const
{
    if (!hasUrl()) {
        return std::move(fallback_value);
    }
    return member_url_;
}

void RTCIceServer::setUrl(const String& value)
{
    member_url_ = value;
    has_url_ = true;
}

void RTCIceServer::setUrl(String&& value)
{
    member_url_ = std::move(value);
    has_url_ = true;
}

String RTCIceServer::getUsernameOr(const String& fallback_value) const
{
    if (!hasUsername()) {
        return fallback_value;
    }
    return member_username_;
}

String RTCIceServer::getUsernameOr(String&& fallback_value) const
{
    if (!hasUsername()) {
        return std::move(fallback_value);
    }
    return member_username_;
}

void RTCIceServer::setUsername(const String& value)
{
    member_username_ = value;
    has_username_ = true;
}

void RTCIceServer::setUsername(String&& value)
{
    member_username_ = std::move(value);
    has_username_ = true;
}

void RTCIceServer::Trace(Visitor* visitor) const
{
    TraceIfNeeded<String>::Trace(visitor, member_credential_);
    TraceIfNeeded<String>::Trace(visitor, member_url_);
    TraceIfNeeded<Member<V8UnionStringOrStringSequence>>::Trace(visitor, member_urls_);
    TraceIfNeeded<String>::Trace(visitor, member_username_);
    bindings::DictionaryBase::Trace(visitor);
}

void RTCIceServer::FillTemplateProperties(WTF::Vector<std::string_view>& properties) const
{
    static_assert(std::size(kOwnPropertyNames) == kOwnPropertyCount);
    properties.AppendRange(std::cbegin(kOwnPropertyNames), std::cend(kOwnPropertyNames));
    DCHECK_EQ(properties.size(), kTotalPropertyCount);
}

void RTCIceServer::FillValuesImpl(ScriptState* script_state, base::span<v8::MaybeLocal<v8::Value>> values) const
{
    CHECK_EQ(kOwnPropertyCount, values.size());
    if (hasCredential()) {
        values[0] = ToV8Traits<IDLString>::ToV8(script_state, member_credential_);
        DCHECK(!values[0].IsEmpty());
    }
    if (hasUrl()) {
        values[1] = ToV8Traits<IDLString>::ToV8(script_state, member_url_);
        DCHECK(!values[1].IsEmpty());
    }
    if (hasUrls()) {
        values[2] = ToV8Traits<V8UnionStringOrStringSequence>::ToV8(script_state, member_urls_.Get());
        DCHECK(!values[2].IsEmpty());
    }
    if (hasUsername()) {
        values[3] = ToV8Traits<IDLString>::ToV8(script_state, member_username_);
        DCHECK(!values[3].IsEmpty());
    }
}

const void* RTCIceServer::TemplateKey() const
{
    static const void* s_key = &s_key;
    return s_key;
}

v8::Local<v8::Object> RTCIceServer::FillValues(ScriptState* script_state, v8::Local<v8::DictionaryTemplate> dict_template) const
{
    v8::MaybeLocal<v8::Value> values[kTotalPropertyCount];
    FillValuesImpl(script_state, values);
    return dict_template->NewInstance(script_state->GetContext(), values);
}

void RTCIceServer::FillMembersFromV8Object(v8::Isolate* isolate, v8::Local<v8::Object> v8_dictionary, ExceptionState& exception_state)
{
    TryRethrowScope rethrow_scope(isolate, exception_state);
    const char* const class_like_name = "RTCIceServer";
    DictionaryConversionContext dictionary_from_v8_context(isolate, class_like_name);
    dictionary_from_v8_context.SetCurrentPropertyName("credential");
    constexpr bool is_optional = false;
    v8::Local<v8::Context> current_context = isolate->GetCurrentContext();
    const auto& v8_own_member_names = GetV8OwnMemberNames(isolate);
    if (!bindings::GetDictionaryMemberFromV8Object<IDLString, is_optional>(isolate, current_context, v8_dictionary, v8_own_member_names[0].Get(isolate),
            has_credential_, member_credential_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("url");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[1].Get(isolate), has_url_, member_url_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("urls");
    if (!bindings::GetDictionaryMemberFromV8Object<V8UnionStringOrStringSequence, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[2].Get(isolate), has_urls_, member_urls_, class_like_name, exception_state)) {
        return;
    }
    dictionary_from_v8_context.SetCurrentPropertyName("username");
    if (!bindings::GetDictionaryMemberFromV8Object<IDLString, is_optional>(
            isolate, current_context, v8_dictionary, v8_own_member_names[3].Get(isolate), has_username_, member_username_, class_like_name, exception_state)) {
        return;
    }
}

const base::span<const v8::Eternal<v8::Name>> RTCIceServer::GetV8OwnMemberNames(v8::Isolate* isolate)
{
    return V8PerIsolateData::From(isolate)->FindOrCreateEternalNameCache(kOwnPropertyNames, kOwnPropertyNames);
}

} // namespace blink
