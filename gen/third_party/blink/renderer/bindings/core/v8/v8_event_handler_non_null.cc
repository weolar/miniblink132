// Copyright 2019 The Chromium Authors
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.

// DO NOT EDIT: This file is auto-generated by
// //third_party/blink/renderer/bindings/scripts/generate_bindings.py
//
// Use the GN flag `blink_enable_generated_code_formatting=true` to enable
// formatting of the generated files.

#include "third_party/blink/renderer/bindings/core/v8/v8_event_handler_non_null.h"

#include <tuple>

#include "base/containers/span.h"
#include "third_party/blink/renderer/bindings/core/v8/callback_invoke_helper.h"
#include "third_party/blink/renderer/bindings/core/v8/generated_code_helper.h"
#include "third_party/blink/renderer/bindings/core/v8/to_v8_traits.h"

namespace blink {

const char* V8EventHandlerNonNull::NameInHeapSnapshot() const
{
    return "V8EventHandlerNonNull";
}

v8::Maybe<ScriptValue> V8EventHandlerNonNull::Invoke(bindings::V8ValueOrScriptWrappableAdapter arg0_receiver, const HeapVector<ScriptValue>& arg1_args)
{
    const char* const class_like_name = "EventHandlerNonNull";
    const char* const property_name = "invoke";
    ScriptState* callback_relevant_script_state = CallbackRelevantScriptStateOrThrowException(class_like_name, property_name);
    if (!callback_relevant_script_state) {
        return v8::Nothing<ScriptValue>();
    }

    v8::Isolate* isolate = GetIsolate();
    if (!IsCallbackFunctionRunnable(callback_relevant_script_state, IncumbentScriptState())) {
        v8::HandleScope handle_scope(isolate);
        v8::Context::Scope context_scope(callback_relevant_script_state->GetContext());
        V8ThrowException::ThrowError(isolate, "The provided callback is no longer runnable.");
        return v8::Nothing<ScriptValue>();
    }

    bindings::CallbackInvokeHelper<CallbackFunctionBase, bindings::CallbackInvokeHelperMode::kLegacyTreatNonObjectAsNull,
        bindings::CallbackReturnTypeIsPromise::kNo>
        helper(this, class_like_name, property_name);
    if (!helper.PrepareForCall(arg0_receiver)) [[unlikely]] {
        if (helper.V8Result().IsEmpty()) {
            return v8::Nothing<ScriptValue>();
        }
        return helper.Result<IDLAny, ScriptValue>();
    }
    v8::Local<v8::Value> argv_arr[10];
    v8::LocalVector<v8::Value> argv_vec(GetIsolate());
    base::span<v8::Local<v8::Value>> argv;
    const size_t argc = 0 + arg1_args.size();
    if (argc <= 10) {
        argv = base::make_span(argv_arr, argc);
    } else {
        argv_vec.resize(argc);
        //argv = argv_vec;
        argv = base::span<v8::Local<v8::Value>>(argv_vec.data(), argv_vec.size());
    }
    ScriptState* script_state = CallbackRelevantScriptState();
    for (wtf_size_t i = 0; i < arg1_args.size(); ++i) {
        v8::Local<v8::Value> v8_arg1_args;
        v8_arg1_args = ToV8Traits<IDLAny>::ToV8(script_state, arg1_args[i]);
        argv[0 + i] = v8_arg1_args;
    }

    if (!helper.Call(static_cast<int>(argv.size()), argv.data())) {
        return v8::Nothing<ScriptValue>();
    }
    return helper.Result<IDLAny, ScriptValue>();
}

v8::Maybe<ScriptValue> V8EventHandlerNonNull::Construct(const HeapVector<ScriptValue>& arg1_args)
{
    const char* const class_like_name = "EventHandlerNonNull";
    const char* const property_name = "construct";
    ScriptState* callback_relevant_script_state = CallbackRelevantScriptStateOrThrowException(class_like_name, property_name);
    if (!callback_relevant_script_state) {
        return v8::Nothing<ScriptValue>();
    }

    v8::Isolate* isolate = GetIsolate();
    if (!IsCallbackFunctionRunnable(callback_relevant_script_state, IncumbentScriptState())) {
        v8::HandleScope handle_scope(isolate);
        v8::Context::Scope context_scope(callback_relevant_script_state->GetContext());
        V8ThrowException::ThrowError(isolate, "The provided callback is no longer runnable.");
        return v8::Nothing<ScriptValue>();
    }

    bindings::CallbackInvokeHelper<CallbackFunctionBase, bindings::CallbackInvokeHelperMode::kConstructorCall, bindings::CallbackReturnTypeIsPromise::kNo>
        helper(this, class_like_name, property_name);
    if (!helper.PrepareForCall(nullptr)) [[unlikely]] {
        if (helper.V8Result().IsEmpty()) {
            return v8::Nothing<ScriptValue>();
        }
        return helper.Result<IDLAny, ScriptValue>();
    }
    v8::Local<v8::Value> argv_arr[10];
    v8::LocalVector<v8::Value> argv_vec(GetIsolate());
    base::span<v8::Local<v8::Value>> argv;
    const size_t argc = 0 + arg1_args.size();
    if (argc <= 10) {
        argv = base::make_span(argv_arr, argc);
    } else {
        argv_vec.resize(argc);
        //argv = argv_vec;
        argv = base::span<v8::Local<v8::Value>>(argv_vec.data(), argv_vec.size());
    }
    ScriptState* script_state = CallbackRelevantScriptState();
    for (wtf_size_t i = 0; i < arg1_args.size(); ++i) {
        v8::Local<v8::Value> v8_arg1_args;
        v8_arg1_args = ToV8Traits<IDLAny>::ToV8(script_state, arg1_args[i]);
        argv[0 + i] = v8_arg1_args;
    }

    if (!helper.Call(static_cast<int>(argv.size()), argv.data())) {
        return v8::Nothing<ScriptValue>();
    }
    return helper.Result<IDLAny, ScriptValue>();
}

bool V8EventHandlerNonNull::IsRunnableOrThrowException(IgnorePause ignore_pause)
{
    ScriptState* callback_relevant_script_state = CallbackRelevantScriptState();

    const bool is_runnable = ignore_pause == IgnorePause::kIgnore
        ? IsCallbackFunctionRunnableIgnoringPause(callback_relevant_script_state, IncumbentScriptState())
        : IsCallbackFunctionRunnable(callback_relevant_script_state, IncumbentScriptState());
    if (is_runnable)
        return true;

    ScriptState::Scope scope(callback_relevant_script_state);
    v8::Isolate* isolate = GetIsolate();
    V8ThrowException::ThrowError(isolate, "The provided callback is no longer runnable.");
    return false;
}

v8::Maybe<ScriptValue> V8EventHandlerNonNull::InvokeWithoutRunnabilityCheck(
    bindings::V8ValueOrScriptWrappableAdapter arg0_receiver, const HeapVector<ScriptValue>& arg1_args)
{
    const char* const class_like_name = "EventHandlerNonNull";
    const char* const property_name = "invoke";
    ScriptState* callback_relevant_script_state = CallbackRelevantScriptStateOrThrowException(class_like_name, property_name);
    if (!callback_relevant_script_state) {
        return v8::Nothing<ScriptValue>();
    }

    bindings::CallbackInvokeHelper<CallbackFunctionBase, bindings::CallbackInvokeHelperMode::kLegacyTreatNonObjectAsNull,
        bindings::CallbackReturnTypeIsPromise::kNo>
        helper(this, class_like_name, property_name);
    if (!helper.PrepareForCall(arg0_receiver)) [[unlikely]] {
        if (helper.V8Result().IsEmpty()) {
            return v8::Nothing<ScriptValue>();
        }
        return helper.Result<IDLAny, ScriptValue>();
    }
    v8::Local<v8::Value> argv_arr[10];
    v8::LocalVector<v8::Value> argv_vec(GetIsolate());
    base::span<v8::Local<v8::Value>> argv;
    const size_t argc = 0 + arg1_args.size();
    if (argc <= 10) {
        argv = base::make_span(argv_arr, argc);
    } else {
        argv_vec.resize(argc);
        //argv = argv_vec;
        argv = base::span<v8::Local<v8::Value>>(argv_vec.data(), argv_vec.size());
    }
    ScriptState* script_state = CallbackRelevantScriptState();
    for (wtf_size_t i = 0; i < arg1_args.size(); ++i) {
        v8::Local<v8::Value> v8_arg1_args;
        v8_arg1_args = ToV8Traits<IDLAny>::ToV8(script_state, arg1_args[i]);
        argv[0 + i] = v8_arg1_args;
    }

    if (!helper.Call(static_cast<int>(argv.size()), argv.data())) {
        return v8::Nothing<ScriptValue>();
    }
    return helper.Result<IDLAny, ScriptValue>();
}

} // namespace blink
